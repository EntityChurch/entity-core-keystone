# Spec & Pipeline Findings Log

Cross-language register of findings surfaced by keystone work. Per-language spec ambiguities live in `protocol-generator/<lang>/status/SPEC-AMBIGUITY-LOG.md`; this log holds findings that are **not language-specific** — spec-level gaps (escalate to arch per the architecture/operators/stewards hand-off boundary, see `AGENTS.md`) and keystone-internal pipeline defects (operators/stewards fix).

**Escalation tags:** `arch` (spec-side resolution) · `research` (profile/process) · `operator` (local fix).

---

## Status sweep (current source of truth)

**Why this section exists**: arch caught itself twice in one session about to draft proposals for already-folded work because the pipe-table cells below carried stale "Open" status. Cells are append-only history; this section is the **current** disposition. Read here first; pipe-table is for provenance.

> **CLOSED (operator), measured 2026-09-02 — F53: a superseded DUPLICATE of the crypto-agility
> corpus was answering, and the peers that transcribed it passed the defect the peers that loaded
> it failed on.**
> Evidence: [`superseded-corpus-duplicate-and-transcribed-pins.md`](../../protocol-generator/shared/findings/superseded-corpus-duplicate-and-transcribed-pins.md)
> — filed with the findings for the same publish reason as F51/F52 below.
> **Not a spec gap and not routed to arch** — the spec rule (§4.5a item 1a: `system/peer` is
> authored at the ECFv1-SHA-256 floor unconditionally) is clear and upstream's corpus already
> encodes it. This is entirely ours: `shared/test-vectors/` carried the corpus twice, and the
> version-stamped copy the peers actually read had been superseded — `hash-format-sha-384.2` was
> INVERTED upstream (the re-hash it pinned is now a construction that MUST be refused) and M3/M6
> `expected_peer_a_content_hash` moved to floor-form. **Nothing failed while both copies existed.**
> **Measured:** `elixir` and `ruby` LOAD the corpus and both failed the same two gates the moment
> the duplicate went — computing `0166f421…` where the corpus wants `00af37ab…`, i.e. the peers
> were deriving a `system/peer` hash under the home format. `ocaml` and `csharp` TRANSCRIBE the
> pins and both **PASSED** (`ocaml` at `RESULT: PASS (25/25)`) while carrying the identical defect,
> because peer and test were wrong in the same direction. All four fixed and re-measured green.
> **`haskell` is predicted to fail and is UNMEASURED** — its spec reads a `canonical_content_hash`
> field the inverted vector no longer has, and the `ghc-toolchain` image cannot resolve its
> test-suite dependencies (`cabal: repoContextWithSecureRepo: unknown repo`), so that peer's S2 has
> no runnable gate on this host. Stated as a source-derived prediction, not a result.
> **No published conformance number moves** — the agility corpus is not in `--profile core`; all
> four peers were and remain `756 · 0F`. That is the point worth keeping: **a second axis was red
> on two peers while the gated axis was green.**
> **Left open on purpose:** the NEGATIVE half. `GUIDE-CONFORMANCE` §2.4a and the current vector both
> ask for an assertion that the forbidden construction is REFUSED; no peer refuses it, and the four
> fixed harnesses assert the floor form without faking the refusal, each saying so at the site.
> Implementing it is a peer-behaviour change (the constructor must reject a non-floor home format
> for one type), not a test edit.
>
> **OPEN, measured 2026-09-02 — F53: conformance assumes a payload capacity the spec never
> states, so a peer can obey §4.10(a) exactly and be recorded as FAILING for it.**
> Evidence: [`conformance-payload-capacity-floor.md`](../../protocol-generator/shared/findings/conformance-payload-capacity-floor.md)
> — filed with the findings, not here, for the same publish reason as F52.
> §4.10(a) requires a **finite** configured maximum and a `413 payload_too_large` above it; the
> framing paragraph says the protocol "places no restriction on entity size" and 16 MiB is a
> **SHOULD**. There is no floor. `concurrency/t1_3_no_head_of_line` stages a **264 109-byte** frame
> (measured on the wire, not read off the vector description, which says "256 KiB"), and a peer
> bounded below that refuses it correctly — whereupon the oracle records a SKIP and prints
> *"skip(s) count as FAIL"*, so obeying the MUST produces `Result: FAIL`.
> **Why it is not just an implementation bug:** capacity is not free on every substrate.
> `cobol` canonicalises with a recursive program whose per-call `LOCAL-STORAGE` holds a 64-entry
> map-pair table, so a 512 KiB value slot costs ~34 MB **per call per nesting level** — measured,
> sustained load dropped 7454 of 10000 requests and the category went 15.5 s → 9 m 50 s. It sits at
> a 32 KiB ceiling, which clears `t1_4` and not `t1_3`. **Ask (arch's call, no wording proposed):**
> state a floor, or make the probe discover the peer's maximum and stage under it, or rule the SKIP
> correct and non-gating and say so in GUIDE-CONFORMANCE. **Our half is already fixed** — the
> oversize path emitted nothing at all, a §4.9(c) drop, now a correlated 413. One peer of 46.
>
> **OPEN, measured 2026-08-30 — F52: §4.7's status table contradicts itself on a pre-hello
> `authenticate`, and the cohort splits 38 / 6 / 1 on the wire.**
> Raised by `entity-core-formalization` (`ROUTING-2026-08-30-PREHELLO-AUTHENTICATE`) from a SOURCE
> census; measured here because `validate-peer` has no vector for the input and they said plainly it
> was unmeasured. Evidence: [`prehello-authenticate-wire-census.md`](../../protocol-generator/shared/findings/prehello-authenticate-wire-census.md)
> — filed with the findings, not here, for the same publish reason as F51 below.
> **The question is theirs, not ours:** §4.7 row 6 says `401 invalid_nonce` (restating §4.6 step 1,
> with the citation), row 10 says `400 connection_sequence_error`, for the same input.
> **What the measurement adds beyond confirming them:** their source read was right on all 34 peers
> it committed to (0 disagreements) and this resolves the 11 it could not. And asked the same
> `authenticate` *after* a hello, only 6 peers answer differently — **the other 39 give the same
> answer either way**, i.e. they never model the pre-hello case and reach row 6's status as a
> by-product of the nonce check. **A 38–6 majority is 6 decisions and 38 fall-throughs**; cohort
> weight should not be cited without that. No conformance number moves. **Sequencing: land the
> ruling before the v0.8.2 regeneration or the ~6-peer sweep is done twice.**
>
> ⛔ **WITHDRAWN 2026-09-01 — F51 was not a spec gap. §1.4 answered it, in the snapshot the finding
> cites in its own header, and we searched for the wrong words.**
> `ENTITY-CORE-PROTOCOL.md` §1.4 line 300 — byte-identical in `v0.8.2` and `v0.8.2.3`,
> `sha256(line) = 376953b9…` — reads *"the path MUST target the local peer's namespace. If the peer
> ID does not match the local peer, the peer MUST reject with status 400 (`invalid_request`)."*
> So the **majority** reading was right and the argument recorded below (that the `peers` default's
> dead weight makes the minority of 6 correct) argued against settled normative text. Both groups
> had the *disposition* wrong: the 40 answered `404 handler_not_found`, the 6 `403`/`200`, the spec
> `400 invalid_request`. What 0.8.2.2/0.8.2.3 added is the code NAME and the explicit prohibition on
> both wrong dispositions — the MUST itself predates the finding, so this is a **withdrawal, not an
> upstream resolution**, and calling it the latter would be the flattering version of the same error.
> **Root cause, and it generalizes past this finding:** the search used the vocabulary of the
> *question* (`peers`, `target_peer`, `check_permission`, `extract_peer`) and the rule is written in
> the vocabulary of *addressing* — it contains none of those four terms. This document even names
> where it expected the answer to be added (§5.2 or §6.6) and never read §1.4, the section it is
> named after. **"The spec does not say" is a claim about the search, and it published as a claim
> about the spec.** Enforcement for the next negative claim about prose: read the SECTION the
> behaviour belongs to, and record which sections you read, so a reader can see the hole instead of
> inheriting the conclusion. Consequences all discharged — the check was deleted rather than
> re-pointed (its PASS branch rewarded the forbidden resolution), its replacement
> `dispatch_inbound_foreign_namespace_refused` passes on 46 of 46, and all six former-PASS peers
> needed the new gate. Correction in the evidence file; the original text is preserved there and
> below, unedited.
>
> **As originally routed 2026-08-30 — F51: the §5.2 `peers` dimension is structurally unreachable in 40 of 46 peers.**
> Evidence: [`peers-dimension-reachability.md`](../../protocol-generator/shared/findings/peers-dimension-reachability.md)
> — filed with the findings, not in `research/stewardship/`, because this register PUBLISHES and
> `research/**` is stripped at release: an index whose evidence is deleted is the documented failure.
> Companion to the earlier [`peers-grant-dimension-oracle-gap.md`](../../protocol-generator/shared/findings/peers-grant-dimension-oracle-gap.md) (2026-08-13), which
> closed the *oracle-coverage* half of this dimension; this is the *reachability* half it exposed.
> **The question:** when an EXECUTE names a URI in a FOREIGN peer's namespace, must a core peer resolve its own
> handler and let §5.2 decide, or may it refuse at routing with `404 handler_not_found`? 40 peers refuse
> (including `go` and all of M1); 6 route it and return a full three-row verdict on `authz_peers_target_from_uri`.
> The split correlates perfectly with one line of routing — no PASS peer has a local-target gate, and every peer
> carrying one WARNs. **The spec does not settle it**, and §3.6's *"`peers` … when absent, defaults to local peer
> only"* is a default that can never deny anything under the majority reading, which is the argument that the
> minority of 6 is correct. Not fixed here: whichever way it resolves, one side of the cohort is non-conformant,
> and deriving the answer from the oracle's Go source would invert the keystone's purpose.
> **Process note carried with it:** this sat in `CONFORMANCE-MATRIX.md` §3 for two weeks as *"WARN on every peer ·
> Low — inconclusive by design · needs a real two-peer harness"* — every clause false, and it was an exculpation
> we wrote about ourselves and never re-checked. One severity-diff across the cohort answered it.

> **Critical-review capstone (2026-07-18):** a four-dimension adversarial review of core (minimality/security/
> design/implementation-history) is complete. Converged view: [`../../protocol-generator/shared/syntheses/synthesis-reconciliation.md`](../../protocol-generator/shared/syntheses/synthesis-reconciliation.md)
> (front door); net-new arch items: [`critical-review-outputs.md`](../../protocol-generator/shared/findings/critical-review-outputs.md).
> **No novel in-core defect found; core is sufficient for its own central paradigm (compute rides the frozen
> core with zero forced change — RT-15) and the coverage well is confirmed dry.** Net-new residue: RT-13 (§4.8
> atomic-refcount + §1.6 frame-write atomicity — two one-line MUSTs), RT-14 (normative lowercase-hex — the
> RT-3 Go-ism receipt), RT-10 (name the continuation `suspend()` seam in core §6.13; its `chain_depth` half is
> in-flight as PROPOSAL-CONTINUATION-BOUNDS-PROPAGATION), a format_code=128 clarifying sentence, an
> `unregister` refcount note, and W6 (mint-time resource absolutization — design proposal). Extension-layer:
> F-PQ (cross-algorithm identity migration → identity/quorum, core not implicated). Editorial: right-size the
> retrospective's minimality verdict (asserted, not proven). Detail:
> [`../../protocol-generator/shared/syntheses/red-team-critical-review.md`](../../protocol-generator/shared/syntheses/red-team-critical-review.md) +
> [`../../protocol-generator/shared/syntheses/implementation-history-review.md`](../../protocol-generator/shared/syntheses/implementation-history-review.md).
>
> **Aggregate digest for arch:** the open spec surface (**F32–F41**, 10 findings across 7 handoffs) plus the retrospective minimality-analysis outputs are consolidated in [`AGGREGATE-F32-F41.md`](../../protocol-generator/shared/findings/AGGREGATE-F32-F41.md) (single front door; this sweep stays source-of-truth on status). After the **full section-by-section core-spec read** (retrospective §8.7, all 4,184 lines): **F42/F43 CLOSED** — float/hash and crypto agility were never in the mandatory core; §9.1 MUST is SHA-256 + Ed25519 only, all agility is §9.3 MAY, so the spec's own MUST/MAY split already *is* the forced-vs-chosen minimality partition. F43 is additionally **downgrade-free by design** (§4.5 makes `key_type` an identity-bound accept-set, not a negotiated single value — no downgrade surface). Three small residues remain: **F44** (narrowed — caveat *mechanism* is §9.1 MUST; open question is only accept-path oracle coverage), **F45** (a one-line §6.5 clarity note on transient request-signature persistence), and **F46** (forward-looking — the `hello` negotiation isn't transcript-authenticated; benign today but a constraint for any future security-bearing negotiated parameter).

| ID | Status | Closing cite | One-line disposition |
|---|---|---|---|
| F1 | ✅ CLOSED | arch `23db254`, ENTITY-CBOR-ENCODING v1.5 | ECF fixture published + cross-blessed 71/71 |
| F2 | acknowledged | arch memo `c0513c8` | MACHINE-SPEC current-or-retire arch-internal |
| F3 | ✅ CLOSED | keystone | Wire-message-model staleness swept |
| F4 | ✅ CLOSED | C# S2 `Ctap2Canonical` verified byte-equal | §3.6-vs-§4.2.1 doc nit remains as cleanup |
| F5 | ✅ CLOSED | arch `23db254` Appendix A.1 patched | Right value derives from new `content_hash.1` |
| F6 | ✅ CLOSED | C-ABI v1.1 `ec_encode_bare_value` test-only export | Direct Class-A differential 71/71 |
| F7 | ✅ CLOSED | V7 v7.73 fold; ECF corpus v1.5→v1.6 with `int.15/16/17` | `2⁶³` / `u64::MAX` / i64::MIN-arg vectors ratified in the v7.73 closeout (proposal §3.3). Corpus bumps to v1.6; SHA published in ENTITY-CBOR-ENCODING Appendix E. |
| F8 | ✅ CLOSED | re-pin 1.0.21→1.0.22-1.fc43 | libsodium S11 discipline |
| F9 | ✅ CLOSED | drop `-fvisibility=hidden`; version script owns exports | C FFI symbol hygiene |
| F10 | ✅ CLOSED | NSec pin 23.4.0→25.4.0 | Profile pinned against live NuGet feed |
| F11 | OPEN (gate-tracked) | spike sequenced post peer #2 sign-off | Native-binding seam; spike scope tracked in the peer-3 (OCaml) arch handoff |
| **F12** | **✅ CLOSED** | **V7 §4.6 line 1859 (v7.62 fold)** | **Nonce-echo MUST landed `401 invalid_nonce`; gated by `connectivity_f12.go`; impl'd by C# `ConnectHandler.cs:153` + TS `connect-handler.ts:137`. Findings-log entry was stale.** |
| F13 | ✅ CLOSED | F13 delegate-request-shape ruling | v7.63 §3.6 + §6.2 already pin shape |
| F14 | ✅ CLOSED | F14 auth-vs-authz status-boundary ruling (CORRECTED — F20 fold) | Table now correctly routes auth-class to 401 |
| **F15** | **✅ CLOSED** | **V7 §4.1 lines 1640-1643 (v7.62 fold)** | **Leg 3 OPTIONAL + reachability-gated; responder MUST NOT push to client-style initiator. Findings-log entry was stale.** |
| F16 | ✅ CLOSED | agility `.cbor` regen + GUIDE-CONFORMANCE §3.2 | `.cbor 4d8dfced→8e7c5232`; decode-and-validate gate now required |
| **F17** | **✅ CLOSED** | **v7.72 §9.5 type floor + oracle `cb54f5b`** | **53-type core floor ratified; type_system gated on core-only under `--profile core`.** |
| **F18** | **✅ CLOSED** | **v7.72 §9.0 core profile + N=2 corroboration (C# + TS PASS)** | **`validate-peer --profile core` machinery shipped; two independent peers reach identical PASS scoreboard. Hand-scoreboard retired.** |
| **F19** | **✅ CLOSED** | **v7.72 §6.7 paragraph (line ~3370) + GUIDE-CONFORMANCE §9** | **§6.5 resolution-first blessed; §6.7 information-leak trade-off documented; ROLE-extension `capability_revoked`/401 vocabulary disambiguated.** |
| **F20** | **✅ CLOSED** | **F14 ruling table correction** | **Auth-class rows (no_author / no_signature / signature_wrong_target / signer_author_mismatch / tampered_signature / author_not_in_included) corrected 403→401; authz-class rows held at 403. Spec was always right (v7.71 §3.3); memo was wrong.** |
| F21 | ✅ CLOSED | oracle `cb54f5b` | `_match` mirrors `_fetch` carve-out under `--profile core` |
| F22 | ✅ CLOSED | oracle `cb54f5b` | EXTENSION-TREE §9 ops op-name-filtered skip under `--profile core` |
| F23 | ✅ CLOSED | keystone `e08b34f` | `PHASE-S4-CONFORMANCE.md` gate redefined as `--profile core` |
| **F29** | **✅ CLOSED** | arch `be54baf` (`nested.5`/`nested.6`); keystone re-vendor `9695b1f1…` | Array-of-maps ≥24/≥256-byte inner-text head-boundary vectors added; cohort re-run **71/71 across 27 codec peers** incl. surfacers Forth/Smalltalk. |
| **F30** | **✅ CLOSED** | arch `be54baf` (`tag_reject.1/2/3/5` regen); keystone re-vendor `9695b1f1…` | `tag_reject` frames now canonical-except-the-mt6-tag → genuinely gate the §6.3 tag scanner (were trailing-data rejects); cohort **71/71**, N2 corpus-gated. |
| **F33** | **Open — surfaced** | `protocol-generator/shared/findings/concurrency-latency-floor-and-cap-sig-coverage.md` (awaiting arch pull) | §6.11 T2.1 "zero drops" completeness assertion imports a **de-facto absolute throughput floor** (fixed 16×10 000 load + fixed 20 s/req client deadline + shared 60 s run `-timeout`) — conflates §4.9(c) *silent drop* with a *conformant graceful slowdown / back-pressure* (§4.9 "not a performance bar / per-language-fair"). Not a peer bug; wasm-wat is GREEN via `--enable-jit`. Ask arch: distinguish late-vs-lost, or model a back-pressurable producer, or document the floor + required compiled execution mode. |
| **F34** | **Open — surfaced** | `protocol-generator/shared/findings/concurrency-latency-floor-and-cap-sig-coverage.md` (awaiting arch pull) | Oracle **coverage gap**: no adversarial vector flips a **granter/capability** signature's detached bytes over an *unchanged* cap `content_hash` on a warm connection (symmetric to `tampered_signature`, which covers only the *author* sig). A peer caching the granter-sig verdict keyed on cap-hash **alone** passes every security/authz check while exploitable. Ask arch: add `tampered_capability_signature`; note the *sound* memoize-by-(hash,signature) optimization as a §4.10(b) headroom lever. |
| **F35** | **Open — surfaced** | `protocol-generator/shared/findings/wasm-dialer-parity-F35-and-execution-mode.md` (awaiting arch pull) | Oracle **coverage gap (outbound-authorization dimension)**: the validator's armed reentry-echo (`handleReentryEcho`, `conformance_handlers.go:84-140` @ `cc1970f`) services the peer's inbound reentrant EXECUTE with **no §5.2 verification at all** — it parses the entity, matches URI+op, echoes `params`. So §7a t1_1/t1_2 pass even if the peer's `dispatch-outbound` originates an **unsigned / unauthorized** outbound EXECUTE — the probe never gates that the outbound seam actually carries the `WithCapability`+`WithIncludedChain` authorization it is *specified* to (`ext/conformance/handler.go`). Symmetric to F34 but on the *outbound origination* surface; same "conformance-green can be vacuous" family. wasm-wat implements the **full authorized** envelope anyway (spec-faithful, not the vacuous minimal one the probe would accept). Ask arch: have the reentry-echo (or a sibling probe) verify author-sig + cap-chain on the inbound reentrant EXECUTE, so §7a gates the outbound seam's authorization. |
| **F36** | **Open — surfaced** | `protocol-generator/shared/findings/asm-018-signature-construction.md` (awaiting arch pull) | **Naming trap (documentation):** the corpus `signature` category signs the entity's **canonical ECF bytes** (`sign(seed, ECF({type,data}))` — the codec-level encode-then-sign primitive), whereas §7.3 signs the **content_hash**. Same word, two different signed messages. Empirically pinned by the asm-L2 differential (golden matched ECF-bytes, not content_hash/digest; `signature.2`'s unsorted data confirms the message is the *sorted* ECF). An implementer reading only §7.3 wires content_hash-signing and fails the corpus `signature` vectors with no pointer. Not a defect (both constructions legit). Ask arch: one-line note distinguishing "ECF-signature" (corpus/codec) from "content_hash signature" (§7.3 protocol). |
| **F37** | **Open — surfaced** | `protocol-generator/shared/findings/F37-peer-id-naming-appendix-b.md` (awaiting arch pull) | Spec-internal contradiction: the peer-identity primitive is named **`system/identity/peer-id`** in the §4.8 bootstrap definitions (and used as a `type_ref` at §10.1-hello/L639/L1082) but **`system/peer-id`** in §10.1 `system/peer` + Appendix B — both live `type_ref`s in one spec version; the oracle's ratified-core set carries only `system/peer-id`. Broader: Appendix B is systemically stale vs the primary ENTITY-CORE-PROTOCOL `:=` definitions (`primitive/any` vs `core/entity`, etc.). Surfaced by **Pure Data #33** (A-PD-012). NOT a peer bug. Ask arch: pick one canonical name + regenerate Appendix B from the primary defs (and fix the §4.4 "14"→15 count). *(renumbered from F33 at the 2026-07-16 WASM/ISA↔Pd/Oz/Io branch merge.)* |
| **F38** | **Open — surfaced** | `protocol-generator/shared/findings/F38-mint-timestamp-precision.md` (awaiting arch pull) | **Mint-timestamp precision is a CORRECTNESS parameter on a content-addressed protocol:** second-truncated `created_at` makes same-scope same-second re-mints byte-identical → same content hash → the oracle's re-mint aliases the §4.4 session floor cap and later categories 403-cascade (**full-profile-only**; isolated `-category` runs stay green). Surfaced by **Pure Data #33** (A-PD-016); keystone fixed (ms wall clock at every mint site). Ask arch: GUIDE-CONFORMANCE note that `created_at` MUST be ms-precision / mints otherwise distinguishable. *(renumbered from F34 at the 2026-07-16 branch merge.)* |
| **F39** | **Open — surfaced** | Folded into the F38 handoff (companion) (awaiting arch pull) | An open/debug seed with bare `resources: ["*"]` **cannot cover foreign namespaces** — §5.5a makes bare `*` granter-local (`/{granter}/*`), so `universal_address_space` writes into `/{fixturePeer}/…` silently **SKIP**; the seed needs BOTH forms `["*", "/*/*"]`. Surfaced by **Pure Data #33** (A-PD-017); keystone seed + shared `seed-policy/README.md` fixed. Residual arch ask: one GUIDE sentence on the dual form. *(renumbered from F35 at the 2026-07-16 branch merge.)* |
| **F40** | **Open — surfaced** | `protocol-generator/shared/findings/authority-as-query.md` (awaiting arch pull) | **§3.6 scope matching is TYPED, but the prose reads as one uniform `matches_scope`:** *id-scope* dims compare literally, *path-scope* dims compare under §1.4 canonicalization. Surfaced by the **SQL peer** (A-SQL-008) as a real ALLOW bug — canonicalizing an operation dim as a path broke a legitimate grant; the fix IS the split. Latent interop hazard (two peers guessing the typing differently disagree on ALLOW for a mixed-dim grant; no vector pins it). Companion A-SQL-007: SQLite `GLOB *` is not segment-anchored (byte-exact for the core pattern set; a `/*/specific` peer-wildcard would over-match). Ask arch: GUIDE clarification that §3.6 matching is typed. |
| **F41** | **Open — surfaced** | `protocol-generator/shared/findings/authority-as-query.md` (awaiting arch pull) | **The §5/§6.6 authority interior is a monotone deductive system; specify the verdict as a DERIVATION so fail-closed + the §5.5a within-grant conjunction become structural invariants, not silently-violable MUST-prose.** Surfaced by the **Datalog peer** (A-DL-013): authored as bottom-up rules, `deny` = absence of a derived `allow` tuple (fail-closed cannot be forgotten on a new code path) and "one grant covers all four dims" = a single join (cannot drift across edits). No wire/verdict change — every peer already computes the same answer. Ask arch: an informative authority-as-derivation appendix to GUIDE-CONFORMANCE. Retrospective: `protocol-generator/shared/evaluations/authority-as-query.md`. |
| **F42** | **Closed — never in mandatory core (§9.3 MAY)** | PROJECT-RETROSPECTIVE §8.3 + §8.7.1 (recorded, no arch ask) | **Float/hash-format is friction confined to the MAY layer, NOT a minimality defect.** First-pass floated the shortest-float ladder as a reduction candidate vs IPLD DAG-CBOR's f64-always. Dismissed: (1) DAG-CBOR chose f64-always for an *uncoordinated* ecosystem (no shared oracle) — the failure mode our keystone+corpus prevents; (2) empirical signal is **absence** (45 substrates, no float defect reached the wire; floats rare-to-absent, timestamps ms-ints F38); (3) per-impl cost, not correctness risk. **Full-spec read closes it:** SHA-256 is the sole §9.1-MUST hash; "additional hash formats" are §9.3 **MAY**. The multi-format / two-address-space machinery (§1.2/§1.2a) is all opt-in; the mandatory core touches none of it. **No arch action.** |
| **F43** | **Closed — minimal-by-construction AND downgrade-free by design** | PROJECT-RETROSPECTIVE §8.4 + §8.7.1 + §8.7.5 (recorded, no arch ask) | **Crypto agility is already minimal-by-construction, not excess — and structurally downgrade-safe.** §9.1 MUST is **Ed25519 only**; "additional key types beyond Ed25519" is §9.3 **MAY** (default core peer = one-curve, FFI-free). The §1.5 `key_type` table reserves the NIST PQ suites (ML-DSA/SLH-DSA/FALCON) — the agility IS the frozen-core PQ-migration mechanism (correcting the earlier "neither is PQ" aside). **The full-spec read also closes the last residual note (downgrade surface):** §4.5 classifies `key_type` as an **accept-set**, NOT a "single active value" — it is *identity-bound* (each peer signs with the fixed key its identity holds; negotiation is only mutual-verifiability). There is no single "connection key_type" to downgrade, so the crypto-agility downgrade attack the literature warns about is **structurally impossible**. WireGuard (a mutable protocol) is the wrong comparator on all counts. **No arch action.** |
| **F44** | **Open — coverage question, NARROWED** (vacuous-green) | PROJECT-RETROSPECTIVE §8.7.4 + AGGREGATE-F32-F41 digest (awaiting arch pull) | **Narrowed by the full-spec read.** The caveat *mechanism* is NOT absent — it is §9.1 **MUST** (constraint key-retention + byte-equality; allowance key-containment + byte-equality, §5.6; delegation-caveat depth/ttl, §5.7). "Non-root thresholds" is NOT a gap — multisig is deliberately **root-only** (§5.5 M3, §9.1). So F44 collapses to one precise question: are the constraint/allowance/delegation-caveat **accept paths** exercised by an oracle vector, or only their reject paths? A MUST mechanism with only rejection coverage is the vacuous-green shape (cf. F34/F35, A-DL-012). §6.13's behavioral-presence precedent is exactly how arch closes such gaps. **Not a defect; blocks nothing.** Ask arch: add an accept-path attenuation vector (constraint added / allowance dropped / caveat depth+ttl honored). |
| **F45** | **Open — clarity note, low priority** (prev. adjudicated impl-side) | PROJECT-RETROSPECTIVE §8.7.4 + AGGREGATE-F32-F41 digest (awaiting arch pull) | **§6.5 `ingest_envelope_signatures` prescribes per-request store growth on memory-primary peers.** The algorithm binds *every* `system/signature` in `envelope.included` at its invariant-pointer path. The request's own author signature has `target == request-root hash` (unique per request) and is consumed inline by `verify_request`, never looked up post-dispatch — so binding it grows a memory-primary store by one entity per request. Two peers hit this independently (Io A-IO-022, Rexx A-RX-014); previously adjudicated impl-side ("spec §6.5 is fine"). Reading the literal algorithm, it *does* prescribe the growth. **Not a correctness defect** (binding is harmless, just wasteful). Ask arch (optional): a one-line §6.5 note — "signatures whose target is the request root are consumed inline and need not be persisted" — spares every future memory-primary peer the rediscovery. **Low priority; blocks nothing.** |
| **F46** | **Open — forward-looking design note, low priority** | PROJECT-RETROSPECTIVE §8.7.5 + AGGREGATE-F32-F41 digest (awaiting arch pull) | **The §4 `hello` negotiation is not transcript-authenticated.** The §4.6 `authenticate` signature covers `{peer_id, public_key, key_type, nonce}` but NOT the negotiated single-active-values (§4.5 hash_formats/compression/encryption). **Benign today:** `key_type` is identity-bound (accept-set, can't be downgraded — see F43); `hash_formats` can only be forced down to the secure SHA-256 floor; compression/encryption are unspecified §9.3-MAY placeholders. **NOT a current defect** — nothing security-bearing is negotiated unauthenticated today. But a real constraint for the future: any security-bearing negotiated parameter added later — notably a *specified* frame-encryption mechanism — MUST be transcript-bound into the `authenticate` signature (or run over an already-confidential transport) to prevent a MITM downgrade. Surfaced by the retrospective's §4 handshake analysis. Ask arch (when frame-encryption is specified): bind negotiated params into the handshake signature. **Low priority; blocks nothing today.** |
| **§PR-8 dispatch surface (V2(a))** | **✅ CLOSED 6-way** | keystone `6099f68`(OCaml) / `bed60c7`(C#) / `0675f9d`(TS) + cohort Go `3aaa0c9` / Rust `4dec10e` / Py `e01fcfb` + arch fold V7 §5.5 NEW E4 + §5.5a granter-frame canonicalization (v7.73 head) | **Dispatch-boundary surface CLOSED 6-way.** Granter-aware cap-resource canonicalization plumbed at the dispatch boundary on all six reference impls. Old→new demonstrated across both halves: V2(a) `captok_form_dispatch_minted_pl_presented_xpeer` ACCEPTED 200 pre-fix (6-way convergent FAIL) → denied 403 post-fix (6-way PASS). Arch folded E4 to V7 §5.5 (cross-peer cap MUST use explicit resource form); §5.2a verdict-to-status enumeration absorbed RULING-F14 inline; V7 bumped to v7.73 head; proposal moved to `proposals/implemented/`. E4 audit: no peer-relative cross-peer helpers found in any keystone peer's test suite. |
| **§PR-8 chain attenuation surface (Amendment 1, V1′)** | **✅ CLOSED 6-way** | keystone `1554a17`(OCaml) / `b123808`(C#) / `c1a3153`(TS) + cohort Go `87e6eaa` / Rust + Py (working-tree) + the V7.73 Amendment-1 (V1′) keystone handoff to arch | **Chain-attenuation surface CLOSED 6-way.** Per-link granter-frame canonicalization in the chain-walk subset-check, gated by the V1′-family triple (`authz_attenuation_foreign_granter_{1,deep,wildcard_leaf}`). Old→new demonstrated 6-way: all ACCEPTED 200 pre-fix → denied 403 post-fix. All three keystone peers are the §3.1 bug class (per-link canon vs local), independently confirmed from code; shipped the §4 **preferred hard-fail** (deny on unresolvable/keyless granter), so `AUTHZ-MALFORMED-GRANTER-IDENTITY-1` is not latent on keystone. Surface 3 (handler-internal re-check on params-derived paths) is **gated indirectly by V2(a) in iterate-all-grants architectures** (cohort + keystone all qualify) → no separate plumbing; `AUTHZ-CONFUSED-DEPUTY-PARAMS-PATH-1` is v7.74 only if a grant-indexed re-check architecture lands. Awaiting arch Amendment-1 fold → v7.73 closes. |

**Remaining open finder-tracked items** (real gaps, not stale):
- **F11** — extensibility-spike gate-tracked, sequenced post-sign-off per parallel-tracks plan.
- **§PR-8 chain attenuation surface** (v7.73 Amendment 1) — ✅ CLOSED 6-way (V1′-family triple); see the row above. Awaiting arch fold → v7.73 closes.
- **v7.74 candidates surfaced this cycle**: `AUTHZ-MALFORMED-GRANTER-IDENTITY-1` (silent-fallback hardening — already pre-empted on keystone via hard-fail); `AUTHZ-MINT-FOREIGN-GRANTER-SUBSET-1` (keystone independent-read: the §6.2 mint subset-check shares the per-link frame question but no vector gates it; kept on local frame deliberately).

**Closed in v7.73 head cycle (dispatch boundary)**:
- **F7** — large-uint ECF corpus additions (`int.15/16/17`) ratified; ENTITY-CBOR-ENCODING corpus v1.6.
- **Mid-chain cap expiry** — `chain_mid_link_expiry_denied` (V1) ratified, 6-way PASS.
- **Cap-token form-dispatch (dispatch surface)** — `captok_form_dispatch_minted_pl_presented_xpeer` (V2(a), load-bearing) + reverse (V2(b)) ratified, 6-way PASS post-fix (caught real authz under-enforcement on 6/6 reference impls at the dispatch surface; chain + handler-re-check surfaces are Amendment 1).

**Lesson installed**: when a finding references a V7 section, **re-grep V7 head before acting**. Spec moves; findings-log cells are append-only history not current state. (Compare arch memory pin [[feedback_ratified_proposal_is_not_folded_spec]] — same shape: ratification ≠ folded; status cell ≠ current.)

---

| ID | Class | Finding | V7 / source | Tag | Status |
|---|---|---|---|---|---|
| **F1** | spec-gap | `ENTITY-CBOR-ENCODING.md` Appendix E (the normative codec conformance artifact) declares its fixture at `core-protocol-domain/specs/test-vectors/ecf-conformance/conformance-vectors-v1.cbor` (+`.diag`) — **that path does not exist** in entity-core-architecture (verified by `find`). | ENTITY-CBOR-ENCODING §E.2 | `arch` | **✅ CLOSED.** Arch produced, cross-blessed (3-way Go/Rust/Py, **71/71 PASS** + live pass-through clean), and published the fixture at the canonical path — arch commit `23db254`, spec bumped to ENTITY-CBOR-ENCODING v1.5. All keystone-requested codec-surface categories (`content_hash`/`peer_id`/`signature`/`envelope`) included; N1 varint vectors landed with no carve-out. Keystone **vendored** the byte-identical copy (SHA `41d68d2d…`) into `test-vectors/v7.56/` with pin `MANIFEST.md`. The conformance-v1 LOCK report for arch records the cross-blessing. The interim oracle is now superseded. *(History: acknowledged arch memo `c0513c8`; gap mapped in the test-vector-reality exploration; request escalated to arch for the ECF fixture, user-carried.)* |
| **F2** | spec-drift | `ENTITY-CORE-MACHINE-SPEC.md` header reads "Version 7.8 (condensed)" while the protocol head is **7.56** (verified). It is a paraphrased derivative, so it is excluded from the spec-data snapshot. | MACHINE-SPEC header vs V7 7.56 | `arch` | **Acknowledged** by arch memo `c0513c8` — arch to bring MACHINE-SPEC current or retire it as superseded. |
| **F3** | pipeline-defect | `PHASE-S3-PEER.md` carried the **stale wire-message model** — L2 row + smoke runner referenced `HELLO / IDENTIFY / QUERY / EXECUTE / ERROR` as wire message types. V7 §3.3 (line 759) is explicit: the **only two wire message types are EXECUTE and EXECUTE_RESPONSE**; `hello`/`authenticate` are *operations* on `system/protocol/connect`, not message types. Left unfixed, every generated peer inherits the wrong model from its own prompt scaffold. | V7 §3.3, §4.1 | `operator` | **Fixed + swept** — corrected in `PHASE-S3-PEER.md`; the same stale model was then found and corrected in two more docs (`spec-data/v7.56/README.md` "Protocol message types" bullet; `validate-peer-usage.md` `connectivity` row). The initial fix was partial — the model had propagated. Now aligned to EXECUTE/EXECUTE_RESPONSE everywhere; `grep -ri 'IDENTIFY\|QUERY.*EXECUTE.*ERROR'` clean. Same staleness arch self-noted in `c0513c8`. |
| **F4** | profile-defect (candidate) | C# profile sets `[codec] cbor_library.canonical_mode = "Strict"`. .NET `CborConformanceMode.Strict` does **not** sort map keys; ECF requires length-then-lexicographic key ordering, so the value is very likely wrong and should be **`Ctap2Canonical`** (and *still* needs a hand-rolled shortest-float pass — no conformance mode does Rule 4). Surfaced by the C# reference-impl review (`research/evaluations/csharp.md` R3). **Ground-truth note:** the byte-identity target (per F1) is the Go encoder `core/ecf/ecf.go`, which uses `fxamacker` `CoreDetEncOptions` = RFC 8949 **§4.2.1** Core Deterministic (bytewise lexicographic on *encoded* keys), **not** §4.2.3 CTAP2. For the core entity maps (all text-string keys, e.g. `data`/`type`) the two orderings coincide, so F4's *direction* (Strict→sorted) is right; the precise empirical gate is "`Ctap2Canonical` output is byte-identical to fxamacker `CoreDet` for the core maps," settled by the `map_keys` vectors. | ENTITY-CBOR-ENCODING §4.1; profile.toml; `ecf.go` | `operator` | **Open (C# profile edit still pending), but the ordering rule is now empirically confirmed.** The rust FFI first pass found the encoder sorts map keys **bytewise-lexicographic on the encoded key** (RFC 8949 §4.2.1, what fxamacker `CoreDet` does) and passed **`map_keys` 6/6** byte-identical to the fixture — confirming F4's direction (Strict→sorted) and that §4.2.1 and CTAP2/§4.2.3 coincide for the corpus's text-key maps. Side note: C-ABI spec §3.6 *describes* the rule as "length-then-lex (CTAP2)", while the producing reference is §4.2.1 — they agree on this corpus but the §3.6 wording should be reconciled to §4.2.1 (minor). C# profile `Strict→Ctap2Canonical` edit still owed when the C# codec runs. **✅ CLOSED (C# S2 first run):** spiked `System.Formats.Cbor` `Ctap2Canonical` against the `map_keys` vectors directly *before* editing the profile (verify-then-edit, S6) — it reproduces the corpus ordering **byte-for-byte**, incl. the 23-vs-24 length boundary (`map_keys.4`) and the mixed bytes/text-key case (`map_keys.5`), confirming §4.2.1 ≡ CTAP2 for this corpus. Profile edited `Strict → Ctap2Canonical`; full native C# codec then passed **map_keys 6/6 + 69/69 overall**. The §3.6-wording-vs-§4.2.1 reconciliation (minor doc nit) remains separately. |
| **F5** | spec-error | `ENTITY-CBOR-ENCODING.md` **Appendix A.1** pins the `empty_map` Wire hash as `44136fa355b3678a…caaff8a`, but `sha256(ECF({})) = sha256(0xA0) = c19a797fa1fd590c…5c7a56a0` — **verified two ways** (Go `crypto/sha256` via the interim oracle + coreutils `sha256sum`). The pinned value disagrees. **No implementation computes it:** Python uses `44136fa3…` only as an *opaque digest fixture* (wire-format roundtrip tests), and `compute_ecf_hash({})` asserts digest *length* only; the value is absent from Go/Rust. Preimage of `44136fa3…` not found among obvious candidates (`0xA0`, empty, `"a0"`, `"A0"`, `0x80`, `0x00 0xA0`). | ENTITY-CBOR-ENCODING Appendix A.1 (present v5/v6/v7) | `arch` | **✅ CLOSED.** Arch patched Appendix A.1 (commit `23db254`). The wrong `44136fa3…` is replaced by the value that **falls out of the new fixture's `content_hash.1`** vector: `005f3139e342f5ef35c1e0eb3140c4511c469d604979d20542bc2ab92fd0ca396b`. **Correction to F5's own diagnosis:** the right value is *not* `sha256(0xA0)=c19a797f…` — `content_hash.1` is over the **full entity** `{type:"system/empty", data:{}}` (canonical ECF `a26464617461a0647479706c73797374656d2f656d707479`, ~25 bytes), so the published content_hash is the 33-byte `00‖SHA256(that)`. Our `0xA0`-preimage hunt was looking at the wrong preimage. The N3 *invariant* (empty map = `0xA0`) was always correct and is unaffected; the disputed-annotation on N3 citations can be lifted. The spec-quality-crawler thesis, confirmed and resolved on the first run. *(Surfaced by the interim dev oracle; escalated to arch in the F1 ECF-fixture request.)* |

| **F6** | conformance-gap (candidate) | The fixture's **Class-A `encode_equal` vectors encode bare CBOR values** (e.g. `float.1`: `0.0 → f90000`), but the codec **C-ABI's only encode entry is entity-shaped** — `ec_encode_ecf(type, data)` (`ENTITY-CODEC-C-ABI-V1.md` §4.1). So the bare canonical-encoder core (which the Class-A vectors test) is **not reachable through the shipped ABI**; an impl-agnostic `dlopen` harness can only drive Class-A transitively (via `ec_content_hash`, whose entity-encode exercises the same int/float/string/map emission). | C-ABI §4.1 vs fixture Class-A | `research` | **Open — surfaced** by the rust FFI first pass. The first-pass harness sidesteps it by linking the crate's safe `api::encode_value` directly (rlib, not `dlopen`). Options for the production 5-way harness: (a) add a `#[cfg(feature="conformance-hooks")]` test-only bare-encode export (not in the shipped ABI); (b) drive Class-A only via `ec_content_hash` and accept transitive coverage; (c) amend the fixture's Class-A vectors to entity-wrap their inputs. Likely (a). Not a spec defect — a keystone conformance-surface design choice; decide before the C-impl/dlopen harness. **Update:** the C FFI first pass + the new impl-agnostic `dlopen` differential (`ffi-generator/c-abi/conformance/abi_differential.c`) now drive the *reachable* ABI surface across both libs (option b in practice — Class-A int/float/string/map emission is exercised transitively via `ec_encode_ecf` + `ec_content_hash`; **48/48 C↔Rust** through the real boundary). The **bare** (non-entity) encoder is still unreachable; option (a)'s test-only bare-encode export remains owed before a *direct* Class-A 5-way differential. **✅ CLOSED (C-ABI v1.1).** Took option (a): added the test-only `ec_encode_bare_value(in,in_len,out,cap,out_len)` export to BOTH impls (decode→re-encode through the bare canonical encoder; canonical-in → canonical-out identity; declared in the header §4.1a, marked "not a protocol surface"). The `dlopen` differential now drives it directly over a Class-A battery (0, large uint, f16, array, 1-key + 2-key maps) — **the bare encoder core is now reachable + cross-validated C↔Rust through the real ABI boundary** (differential **71/71**). The direct Class-A differential F6 wanted now exists. |

| **F7** | corpus-coverage-gap | The fixture's `int` category tops out at **`int.10` = 9223372036854775807 (i64::MAX)**; negatives reach only `-256`. **CBOR major-type-0 uints in `[2^63, 2^64-1]` are valid but untested** — and that range is exactly where an impl that decodes uints into a *signed* 64-bit int silently overflows (a classic codec bug class). No vector would catch it. | fixture `int` category vs RFC 8949 mt-0 range | `arch` | **Open — surfaced** by the rust FFI first-pass review. Rust impl is correct (u64/i128 throughout) and now has a local regression test (`encode.rs::tests::uint_above_i64_max`, pins `2^63 → 1b8000000000000000`, `u64::MAX → 1bffffffffffffffff`), but conformance does not prove it. **Ask arch:** add additive `int` vectors for `2^63`, `u64::MAX`, and a large-arg negative / i64::MIN. Meanwhile the C FFI MUST use `uint64` (not int64) for mt-0; mirror the rust regression test. Demonstrates S8 in practice: conformance-green proves correctness *against the test surface*, not across the whole type domain. **Update:** C FFI mirrored it and went one better — the C value model stores ints as `(negative, uint64 arg)` (the raw CBOR head argument), so there is **no signed integer to overflow** structurally. Regression test `regression_test.c` pins `2^63 → 1b8000000000000000`, `u64::MAX → 1bffffffffffffffff`, `i64::MIN → 3b7fffffffffffffff` (12/12 green). Arch ask for additive `int` vectors still stands. |

| **F8** | supply-chain-repin | `research/evaluations/ffi-c.md` pinned **libsodium 1.0.21** for the C codec. fedora:43's repos ship **only `1.0.22-1.fc43`** (~2 months old at pin time, clears the S11 30-day floor). The eval predates seeing the distro's actual contents. | S11 pin discipline; `ffi-c.md` stack table; `containers/c-toolchain/` | `operator` | **Resolved — deliberate, reviewed re-pin** 1.0.21→**1.0.22-1.fc43**. Rationale: 1.0.22 is a *strict superset* of 1.0.21 (adds ML-KEM/SHA-3; the high-level `crypto_sign_*` / `crypto_hash_sha256` we use are unchanged → **conformance-neutral**, confirmed by 48/48 C↔Rust dlopen agreement on `ec_sha256`/`ec_ed25519_*`). CVE-2025-69277 is in the low-level `crypto_core_ed25519_is_valid_point` validator we do **not** call. Pinned as exact NVR in `containers/c-toolchain/Containerfile`; `ffi-c.md`'s 1.0.21 line is now interim history. This is the S11 "re-pinning is deliberate and reviewed" path, not incidental drift. |

| **F9** | guide-refinement | `ffi-c.md` "symbol hygiene" lists `-fvisibility=hidden` **and** a version script / `__attribute__((visibility("default")))` as if complementary. But the canonical `entitycore_codec.h` is shipped **verbatim** (S2-style) and carries no visibility attributes — so compiling with `-fvisibility=hidden` makes the `ec_*` exports hidden, and a hidden symbol **cannot be promoted back** by the version script's `global:`. Result: a `.so` with **zero exports** (observed first build). | `ffi-c.md` build-gotchas; `export.map` | `research` | **Resolved.** Drop `-fvisibility=hidden`; let the linker **version script alone** control exports (`{ global: ec_*; local: *; }`) — it exports `ec_*` and localizes everything else (libsodium symbols included). Verified: `nm -D` shows exactly the 19 ABI symbols, 0 leaks, `.so` self-contained (`ldd` = libc only). `ffi-c.md` should be amended: with a verbatim header, `-fvisibility=hidden` is *actively harmful*, not complementary. Also renamed internal `ec_buf*` → `ecbuf*` so internal helpers don't match the `ec_*` export glob. |

| **F10** | profile-defect | C# profile `[codec] ed25519_library.version = "23.4.0"` — **NSec.Cryptography has no stable 23.4.0**; the 23.x line is previews only (`23.5.0-preview.1` … `23.9.0-preview.3`). Stable cadence is 22.4.0 → 24.4.0 → 25.4.0 → 26.4.0. `dotnet restore` silently bumps the bad pin to 24.4.0 (NU1601 → error under warnings-as-errors). | `profile.toml`; NSec NuGet feed | `research` | **Resolved (C# S2).** Corrected pin → **NSec 25.4.0** (stable, ~13mo old → S11-clean, net9-compatible, pins libsodium 1.0.20.1). Chosen over 26.4.0 (only ~38d old — uncomfortably near the S11 floor) and 24.4.0 (pre-net9). Ed25519 is deterministic (RFC 8032) → version-neutral for conformance; confirmed by `signature.*` 3/3 byte-identical to the Go/Rust/Py-blessed seeds. Profile edited inline; logged `protocol-generator/csharp/status/SPEC-AMBIGUITY-LOG.md` A-002. **Process lesson:** profile library pins should be validated against the live registry at authoring time — a non-existent pin is a hard stop under warnings-as-errors. (Base58 was also unpinned → hand-rolled, A-003.) |

| **F11** | guide-refinement (candidate) | V7 §6.1 leaves the mechanism for binding **language-native executable code** to a registered `system/handler` manifest implementation-defined ("SDK-OPERATIONS §11.6 specifies the SDK-level contract"). This is fine for the spec, but it is a **per-language design point every generated peer must solve** to implement the handlers handler's `register` (§6.2) for native (non-`expression_path`) handlers — and it is invisible until you try to dispatch to a registered handler. A generated peer that installs the manifest entities but has no code-binding registry will register handlers that resolve in the tree-walk yet have nothing to run. | V7 §6.1, §6.2; SDK-OPERATIONS §11.6 | `research` | **Open — surfaced** by the C# S3 peer (A-007). The C# peer's `HandlerRegistry` already pairs a pattern→`IHandler` map with the tree entities for its *bootstrap* handlers; the same seam generalizes to `register` (a per-pattern factory/delegate registry, distinct from the entity-native `expression_path` route). Worth a `diagnostics/` note so the next language's operator designs the native-binding registry up front rather than discovering it at first `register`. Not a spec defect. |
| **F12** | spec-gap (candidate) | V7 §4.6 (authenticate signature) specifies reconstructing the authenticate entity and verifying its signature, but does **not** state that the receiver MUST check the **echoed nonce** equals the nonce it issued in its own hello. §3.8 documents `authenticate.nonce` as "other peer's nonce echoed back," establishing intent, but the §4.6 verification algorithm omits the equality check. Without it, an authenticate signature captured on one connection is replayable on another — the nonce binds the signature to *a* challenge, but the verifier never confirms it is *its* challenge. | V7 §4.6 vs §3.8 | `arch` | **Open — surfaced** by the C# S3 connect handler (A-008). The C# peer verifies the signature but does not yet validate the echo (the sent nonce is not retained per-connection). **Ask arch:** make the nonce-echo equality check explicit in §4.6 (receiver retains its issued nonce per-connection; rejects on mismatch with `invalid_nonce`/401). Classic spec-quality-crawler payoff: a security-relevant step that "everyone knows" but the normative algorithm omits. |
| **F13** | spec-gap (candidate) | V7 §3.6 / §6.2 describe `system/capability:delegate` as "accepts an existing `system/capability/token` (the parent) and returns a new token attenuated from it" — but the operation's **input shape is just the parent token**, with no field naming the **grantee** of the delegated child. A delegated token MUST have a grantee (§3.6 grantee is required and per-link resolvable, §5.5); where the caller specifies it for `:delegate` is unspecified. (Contrast `:request`, whose grantee is the EXECUTE author.) | V7 §3.6, §6.2 (`delegate` op) | `arch` | **Open — surfaced** by the C# S3 capability handler (A-006, which returns 501 for `delegate` partly for this reason). **Ask arch:** define the `:delegate` input — either a dedicated `system/capability/delegate-request {parent, grantee, ...}` per the §3.7 `{handler}/{op}-request` convention, or normatively state that the grantee is carried elsewhere (e.g. the EXECUTE author = self-delegation, or a params field). Until resolved, conformant `:delegate` cannot be implemented unambiguously. **✅ CLOSED** — the F13 delegate-request-shape ruling: NO SPEC CHANGE. V7 §3.6 (lines 1154–1180) + §6.2 (line 1204) already pin `delegate-request {parent, grants, ttl_ms}` self-attenuation + same-peer-only + 501-on-cross-peer (v7.63 F1). Keystone's read predated v7.63. |

| **F14** | spec-gap (candidate) | The 401-vs-403 boundary between **connect-time authentication** (§4.6) and **request-time capability/signature verification** (§5.2) is not stated explicitly. V7's status tables give 401 "authentication failed" and 403 for missing/insufficient authorization, but do not pin which applies to a *request* (post-handshake EXECUTE) whose **signature is invalid / capability is not in `included` / signer≠author / grantee≠author**. `validate-peer`'s `security` category treats all five as **403** (V7 §5.2); a peer can plausibly read a bad request signature as an *authentication* failure (401). Without a normative statement, conformant peers diverge on the code for the same rejection. | V7 §4.6 vs §5.2; status tables §6.12 | `arch` | **Open — surfaced** by C# S4 (`security` 10P/5F, every fail `expected 403, got 401`). The C# peer *correctly rejects* all forgeries but returns 401 for request-time signature failures. **Ask arch:** state in §5.2 that request-verification failures (bad/absent signature, cap not in `included`, signer/grantee mismatch) are **403** (authorization), reserving 401 for §4.6 connect-time authentication. Peer fix (401→403) is queued regardless — `validate-peer` is the oracle (S5). **⚠️ CONTESTED-CLOSE → see [F20].** `RULING-F14` closed this NO-SPEC-CHANGE and the reply derived "fix C# 401→403". **But the v7.71+ oracle (`security.go`) requires the OPPOSITE: request-time signature/author/signer-mismatch → 401 (auth-class).** The original "all five as 403" read was against the *pre-v7.71* oracle, which v7.71 §3.3 then re-pointed to 401 (auth-class). Go/Rust/Python pass `security` at 401; C# already returns 401 and passes. **No peer change made** — applying the reply's 401→403 would regress 4–5 security checks. The F14-memo table needs correction, tracked as F20. |
| **F15** | spec-gap (candidate) | §4.1's mutual handshake (3 EXECUTE + 3 EXECUTE_RESPONSE) has the **responder send a reverse `authenticate` (leg 3)** to the initiator. But the diagram is silent on whether a responder MUST **proactively push** leg 3 to *any* initiator, including a **client-style initiator that only does request/response and never demuxes inbound EXECUTEs**. `validate-peer` (the reference client) never consumes leg 3 — its handshake reads exactly leg-2's response and stops, and its category loop reads the unsolicited leg-3 EXECUTE as a spurious response (status 0). The reference peers pass the full suite, which implies the conformant responder does **not** unilaterally push leg 3 to such an initiator (or leg 3 is initiator-driven/optional). | V7 §4.1 flow diagram; §4.8 inbound-concurrency | `arch` | **Open — surfaced** by C# S4. After fixing leg-3 *ordering* (Finding 0 — leg 3 must follow leg 2's response), the residual proactive push still corrupts the validator's first post-handshake read (`type_system` `types_listing_available: status 0`). **Ask arch:** clarify §4.1 — is the reverse authenticate responder-pushed unconditionally, or initiator-driven / gated on the initiator being a full peer? Until then the C# peer's `RespondAsync` proactive leg-3 send is the likely fix target (make it initiator-driven). Note the tension with §4.8 (inbound concurrency is a MUST for full peers, but a *test client* isn't a full peer). |
| **F16** | corpus-defect (the byte gate doing its job) | The **locked crypto-agility corpus** `test-vectors/v0.8.0/agility-vectors-v1.cbor` (SHA `4d8dfced…c6d0ae`, the cohort lock, byte-equal Go×Rust×Py per the MANIFEST) is **internally inconsistent with its own `.diag`** in three ways. The `.diag` prose was hand-advanced to a Phase-2-LOCKED narrative with real hex pins, but **the canonical `.cbor` build artifact was never regenerated from it** — so the bytes keystone must consume (S2/S5) are stale/corrupt. **(A)** Every Ed448 seed (`key-type-ed448.1/.4`, all `matrix.*` peer A) is encoded as **58 bytes** (`0x42×58`); RFC 8032 Ed448 seed is **57** (and `agility-SEEDS.md` itself says "`0x42×57`"). **(B)** Every Phase-2 `matrix.M2/M3/M6` `expected_*` value (pubkeys, content_hashes, peer_ids, cap content_hash, signature — 8 string fields) is still the placeholder **`"TBD-COHORT-ROUND-TRIP"`**, not a byte pin — so **G1 is only Phase-1-closed, not Phase-2**. **(C)** The `experimental-test` fixture's `public_key` (`hash-format-sha-384.1/.2`) is encoded as **63 bytes**; the description + the inherited v7.66 pin are for **64** (`0xAA×64`). | `agility-vectors-v1.cbor` vs `.diag`/`SEEDS.md`; RFC 8032 / V7 §1.2,§1.5 | `arch` | **✅ RESOLVED — regenerated, re-vendored, decode-validated.** Surfaced by the FFI Rust agility harness (C-ABI v1.1). **The keystone codec was proven byte-correct** despite the corpus: feeding the *intended* RFC inputs (57-byte seed, 64-byte pubkey) reproduces every Phase-1 `canonical*` pin exactly — `verify` bin **5/5**. The agility harness drove **6/6 clean codec checks** and reported the 7 defective vectors as **BLOCKED — not doctored to pass (S5)**. **Arch disposition** (the F16 agility-corpus CBOR-regen closeout): regenerated `.cbor` from the (always-correct) `.diag` via idempotent `tools/regen-v767-cbor.py` — **no crypto pin changed**, only input-side widths + Phase-2 `expected_*` field types. **Keystone re-vendor (this session):** `.cbor 4d8dfced…c6d0ae → 8e7c5232…f31f982e` (0 TBD), `.diag 05b53e2d…aafc5d → 81cea818…961818` (footer-only delta), MANIFEST sha rows re-stamped with supersession note. **§3.2 decode-and-validate gate (the new GUIDE-CONFORMANCE requirement) PASSED with zero codec change:** `agility_harness` **28/28 clean** (was 6/6+7 BLOCKED) incl. `matrix_flow 18/18` (Phase-2 now byte-real), `verify 5/5`, ECF `69/69` (Rust+C), C↔Rust differential `71/71`. The lone SKIP (`varint-reserved-ff.1.key_type`) is the deliberate mint-level/out-of-codec-scope check (S3, not silent). G1 now **Phase-2 closed**. Reopened "G1 CLOSED" lesson: `.diag` had 0 TBD (what the prior session checked) but the `.cbor` had 8 — only a byte-level decode of the artifact catches it; now wired into GUIDE-CONFORMANCE §3.2. Full proof + repro in the agility-corpus request to arch. |
| **F17** | oracle-over-demand (the refined G4) | `validate-peer`'s `type_system` category builds its comparison registry from `types.RegisterCoreTypes` + the connect/tree handler types (`runTypeSystem`, == `compare-types` `generateLocalTypes`) and demands a byte-exact `system/type/<name>` from the peer for **all 150** of them (only `system/substitute/*` is downgraded to provisional/WARN via `isProvisionalType`). But **~97 of the 150 are extension vocabulary or the *type extension*** — not core. A **core** protocol peer publishes core + operational + the type-system **bootstrap** (`system/type`, `system/type/field-spec`, `system/type/name`) only; extensions bring their own types **and** handlers when installed (publishing a type *shape* ≠ implementing the extension). The reference Go peer publishes all 150 (`seedFromRegistries`, `peer.go:740`) because it bundles every extension — so the oracle's floor follows suit and **over-fails a correct core-only peer** on ~93 hard FAILs (the 97 minus the 4 already-provisional substitute types). Concretely the over-demand spans: COMPUTE (`compute/*` ×20 + `system/compute/*` ×8), CLOCK ×9, CONTENT ×3, CONTINUATION ×9, DURABILITY ×3, QUERY ×7, SUBSCRIPTION ×5, INBOX ×2, transport/http-poll ×1, and the **type extension** ×26 (`system/type/constraint/*`, `validate*`, `violation`, `compare/compatible/converge/adopt/reconcile/field-*`). | `validate-peer` `cmd/internal/validate/typesystem.go`; `core/types/core.go` `RegisterCoreTypes`; TYPE-SYSTEM §2.7, §8–§10 | `arch` | **Open — surfaced** by C# S4 type_system bring-up (supersedes the earlier "A3/F2 type_system over-scope" read, which guessed ~250 oracle / ~40 impl without the registry breakdown). **Deliverable produced:** the principled **53-core / 97-extension** manifest in `protocol-generator/csharp/status/S4-TYPE-SCOPE.txt`, derived from the full 150-type render (`test-vectors/v0.8.0/type-registry-vectors-v1.{cbor,diag}`, generated by `protocol-generator/shared/tools/dump-type-registry`). **Ask arch:** give the `type_system` oracle a *ratified-core type floor* (a manifest of the ~53 core types) so a core peer is scored only on those, with extension types treated like the handlers-extension exclusions (present-and-matched if installed, not-a-FAIL if absent) — analogous to the existing `isProvisionalType` carve-out but for the whole extension set, and to the handler `-exclude` list. **Keystone interim:** the C# peer renders + publishes the 53 core types natively (reflection + override table, byte-exact vs the vector set) and **excludes the 97 extension demands from the core verdict** (per-category core read, not blind-conform — S5). **Blurry edges flagged for arch ruling:** `system/delivery-spec` (referenced by core `ExecuteData` → kept core, but also feeds subscription/continuation), `system/capability/{delegate,revoke}-request` + `policy-entry` (core cap-management vs an extension?), `system/deletion-marker` (confirm deletion is core). Classic crawler payoff: the oracle's "core" set was never separated from its "everything the reference peer happens to ship" set. |
| **F18** | suite-scope (the core-peer test surface) | `validate-peer` has **no core-peer verdict** — it scores every peer against the full standard-peer surface, so a *correct, complete core peer* reports as massively failing. A full-suite run of the C# core peer is **271 pass / 771 fail / 113 skip across 38 categories**, yet nearly all of the 771 fails are extension demands on a peer that (by CLAUDE.md scope) must not implement extensions. Two distinct over-demand shapes: **(a) whole extension categories** — `subscriptions, continuations, revision, auto_version, clock, history, query, compute, entity_native, attestation, quorum, identity, role, behavioral_role, behavioral_v33, durability, type` (the type extension), `content, serving_mode, universal_address_space, transport_family, session` — a core peer has none of these handlers, so the categories are ~all-FAIL by construction; and **(b) extension demands inside nominally-core categories** — e.g. `handlers` bakes EXTENSION-TREE §9 ops (`snapshot/diff/merge/extract`) into the tree handler's required op-set (`handler_tree_operations_match` FAILs) and lists 4 extension handlers (inbox/continuation/subscription/revision) among the 7 "expected"; `type_system` is F17; `tree_operations` is ~all extension ops bar `path_reject_empty_segment`; `authz` couples some checks to EXTENSION-ROLE (`system/role:delegate`). The current practice (per-`-category` reads + a hand-maintained mental exclusion list) does not scale and is not reproducible. | `validate-peer` `suite.go` category set; `handlers.go` `expectedHandlers`; CLAUDE.md scope (extensions out) | `arch` | **Open — surfaced** (user-requested escalation during C# S4). **Ask arch:** define a **core-peer conformance profile** — the set of categories + per-category check subsets that a spec-compliant *core* peer must pass (extension-free), so `validate-peer --profile core` (or equivalent) yields a clean PASS/FAIL a core peer can gate on. Fold in F17 (type_system core type-floor) and the `handlers` core-op/core-handler subset. This is the natural completion of the keystone's whole premise: the oracle is the contract (S5/S7), but there is currently no *core* contract — only the full-peer one. **Keystone interim:** maintain the core scoreboard by hand (per-category core reads, documenting which fails are over-demand) until the profile exists. **Related to F17** (a subset of this finding, kept separate because it has a concrete deliverable already). Also a place to add the user's wish for **explicit tree put/delete/flex tests** in the core profile. **Update (TS peer #2 S4):** the `--profile core` machinery now exists (the F21/F22 fixes shipped in oracle `cb54f5b`) and is **independently corroborated** — TypeScript peer #2, a structurally-different runtime, reaches the *identical* core verdict the C# peer #1 reached (`552 total / 269 pass / 194 warn / 0 fail / 89 skip`, all 89 skips §9.0-auto-allowlisted) on its **first** validate-peer run, zero code fixes. The hand-maintained core scoreboard is fully superseded by the machine PASS for both peers. The §9.0 profile is doing exactly what F18 asked for. |

| **F19** | spec-resolved + oracle-over-demand (the §5.4 ordering question, closed) | The handoff's open **§5.4 dispatch-ordering** question — *does dispatch-level authorization precede handler resolution (auth-first → 403), or is resolution-first (404) acceptable for an unauthorized request to an uninstalled handler?* — is **resolved by V7 §6.5**, which pins the order unambiguously: `Resolve handler from tree (§6.6) → no match → 404` comes **before** `Check permission (§5.2 check_permission) → 403 on deny`. §5.2 reinforces it: "Permission check … is *called after handler resolution* in the dispatch chain (§6.5), which provides the handler pattern needed for handler scope checking." So **resolution-first (404) is the conformant order**; auth-first would contradict §6.5. The consequence is that three oracle checks over-demand a core peer by routing through *extension* handlers a core peer doesn't install: **(a) `security.handler_scope_denied`** targets `system/subscription` (SUBSCRIPTION extension) with a `system/tree`-scoped cap — the reference peer 403s only because it bundles the subscription handler; a core peer correctly 404s (handler not found) before the handler-scope check can fire. **(b) `authz.authz_delegate_grant_1`** targets `system/role` (ROLE extension) — a core peer 404s; the check expects 403 `capability_denied` from an installed role handler. **(c) `authz.authz_revoked_1`** demands the ROLE §5.5 in-flight-cascade code **401 `capability_revoked`**, where a core peer's §5.1/§5.2 revocation denial is the core default **403 `capability_denied`** (which the keystone now emits + the companion `capability.revoked_cap_denied_on_use` check accepts). | V7 §6.5 (dispatch chain), §5.2 (check_permission post-resolution note); `validate-peer` `security.go handler_scope_denied`, `authz.go authz_delegate_grant_1`/`authz_revoked_1` | `arch` | **Open — surfaced** (C# S4 core close-out, spec-first). **Disposition: NOT a peer bug — §6.5 mandates resolution-first; the keystone peer is conformant (404 / 403-default).** These three are **F18-class over-demands** and are excluded from the core verdict. **Ask arch:** (1) bless the §6.5 resolution-first ordering as the answer to the §5.4 question (and note the information-leak trade-off it implies: an unauthorized request to an *uninstalled* handler reveals non-existence via 404 rather than masking it with 403 — acceptable, but worth an explicit §6.7 sentence); (2) the F18 core-profile MUST route handler-scope-denial / delegate-grant / revocation-denial checks through **core** handlers (e.g. a `system/tree`-scoped cap denied against `system/capability`, not `system/subscription`) so a core peer is gated on the behavior, not on extension-handler presence; (3) confirm `capability_revoked`/401 is ROLE-extension vocabulary and the core revocation denial is `capability_denied`/403 (the §5.2 step-4 default). **Keystone interim:** wired §5.2 step-4 `is_revoked` over the full chain (403 capability_denied), the §5.2 PR-3 leaf `unresolvable_grantee`→401 carve-out, and the §6.2 `request`/`configure`/`revoke` bodies — driving capability 12/12, peer_canonicalization 7/7, authz 5/7-core (2 = these F18 ROLE over-demands), security 21/22 (1 = (a)). |

| **F20** | ruling ↔ oracle contradiction (auth-vs-authz status) | The `RULING-F14` disposition table lists request-time **bad/absent signature** and **signer≠author** as **403 `capability_denied`**, and the `REPLY-FROM-ARCH-V7.72` derived the instruction *"fix the C# peer's request-time 401s on signature-failure to 403."* **The v7.71+ oracle requires the opposite.** `validate-peer/security.go:26-29` + `sendAndExpectAuthDeny` (`security.go:243-264`) pin **authentication-class failures (signature / author / signer-mismatch) at 401**, with the source-compat note "pre-v7.71 Go emitted 403 … v7.71 §3.3 reroutes auth-class to 401; a 403 is now a real regression." The Phase-1 checks (`checkNoSignature`/`checkSignatureWrongTarget`/`checkSignerAuthorMismatch`/`checkTamperedSignature`/`checkNoAuthor`/`checkAuthorNotInIncluded`) all route through `sendTamperedExecute` → **expect 401**; only `checkNoCapability`/`checkCapabilityNotInIncluded` expect 403. Go/Rust/Python pass `security` (0 fail) → **401 is the conformant behavior**. C# already returns 401 (passes). The memo table mis-assigned §4.6/§3.5 *authentication* failures to the §5.2 *authorization* 403-path. | F14 ruling table vs `validate-peer/security.go`; V7 §3.3 (v7.71 auth-class) vs §5.2 (verify_request DENY) | `arch` | **Open — surfaced** by C# S4 `--profile core` re-run. **No peer change made** — applying the reply's instruction would have regressed 4–5 passing security checks (the whole point of running the oracle caught it). **Ask arch:** correct the F14-memo rows for request-time signature/author/signer-mismatch from 403 → **401 (auth-class)** and reconcile the reply; or, if §5.2 is intended to pull signature verification *inside* `verify_request` (making 403 right), that's a much larger change to the oracle + Go/Rust/Python. Routed in the core-profile oracle-gaps report to go. |
| **F21** | oracle bug (`--profile core` type_system) | `validate-peer/typesystem.go` `_match` leg (lines **124–127**) returns a hard `FailCheck("remote type definition not available")` when a non-core-floor type is absent under `--profile core`, even though the paired `_fetch` leg (lines 88–101) correctly `WarnCheck`s the same absence (matched-if-present carve-out). The provisional downgrade (lines 133–139) sits *after* the early `return` at 126, so it never applies to the absent case. Result: a **true** core peer (publishes the 53-type §9.5 floor, not 150) hard-fails the **97** absent extension types → 97 fail + 97 warn. The v7.72 cohort never caught it because Go/Rust/Python are **full** peers that publish all 150, so `remoteDef` is never nil. | `cmd/internal/validate/typesystem.go:124-127` (+ the existing `_fetch` carve-out at 88-101 as the pattern) | `go` (oracle owner) | **Open — surfaced** by C# `--profile core` (first true core peer). **NOT a peer bug.** Proposed minimal fix (mirror `_fetch`: `if provisional { return WarnCheck(...) }`) in the core-profile oracle-gaps report to go. After fix: type_system 108 pass / 194 warn / 0 fail for a core peer. |
| **F22** | oracle bug (`--profile core` tree_operations) | `validate-peer/tree_operations.go` does **not** skip the EXTENSION-TREE §9 ops under `--profile core`, contradicting V7 §9.0 (spec text ~L3720: *"EXTENSION-TREE §9 ops (snapshot, diff, merge, extract, tracked) are skipped under --profile core"*) and the cohort handoff's stated intent. A core peer correctly returns 501 for `snapshot`/`extract`/`merge`/`diff`/`tracked`; the oracle marks them FAIL and cascades `blocked: depends on …` to dependents → **29** tree fails. **Subtlety for the fix:** `put_entity`/`get_entity`/`listing_get_roundtrip_*` are *labeled* `EXTENSION-TREE S1/S2` but are **core** get/put/list (they PASS) — so the carve-out MUST filter by **op-name** (`snapshot_`/`diff_`/`extract_`/`merge_`/`roundtrip_`/`tracked_`/`tracking_`), not by the `EXTENSION-TREE` spec-ref prefix. | `cmd/internal/validate/tree_operations.go` (~L1359 return); V7 §9.0/§9.5a/§3720 | `go` (oracle owner) | **Open — surfaced** by C# `--profile core`. **NOT a peer bug** (EXTENSION-TREE is out of core scope, CLAUDE.md). Proposed op-name skip (auto-allowlisted via the `"V7 v7.72 §9.0"` message prefix → `result.go:170 isProfileKeyedSkip`) in the core-profile oracle-gaps report to go. |
| **F24** | spec-stubbed-vs-MUST + oracle blind-spot (handler registration) | `system/handler:register`/`unregister` is **core protocol** (V7 §6.2; `register-request`/`register-result` types §3.12) and a **MUST per §6.9**, yet all three keystone peers **501-stub it** (A-007). It slipped because `--profile core` **never exercises it**: the `handlers` category is pure static manifest introspection (`TreeListing`+`TreeGet`, never a `register` EXECUTE); dynamic `register` is invoked only by `entity_native`/`compute`/`convergence`, all excluded from core. The §9.5 type-floor forces a core peer to *publish* the register-request/result **vocabulary**, creating a false sense of coverage — the types are gated, the **behavior** is never invoked. So "can someone pull in a generated core peer and install a custom handler?" is currently **no, not via the protocol surface.** The internal native-binding seam (`HandlerRegistry.register()` + §6.6 dispatch) already works for bootstrap handlers; the build is wiring the protocol op to it + the §11.6 collision(409)/compensation/ordering MUSTs. Per SDK-OPERATIONS §11.6 a conformant peer may build the **V1.0 (L0, peer-owner, no cap-check)** paired-write now, with **V2.0 (L1 cap-checked)** as a flagged follow-on. | V7 §6.2/§6.9/§3.12; SDK-OPERATIONS §11.3/§11.6; `validate-peer handlers.go`/`entity_native.go`/`profile.go` | `keystone` (peer build) + `go`/`arch` (oracle core-tier register check) | **Open — surfaced** by the core-protocol extensibility-boundary exploration (§3/§5). **Decision:** implement `system/handler:register` in the core peers (V1.0/L0) wired to the existing registry; **ask arch** to confirm V1.0-now/V2.0-later; **ask go** to add a core-tier dynamic-register check so the gate proves the seam (would have caught A-007). Closes the original "extensibility spike" *as a real core capability*, not a `test/echo` throwaway. |
| **F25** | core-MUST misclassified + machinery gap (handler outbound dispatch) | Handler-initiated **outbound dispatch** is **core and MUST-level, profile-independent** — §4.8 ("Implementations MUST support inbound frame processing concurrent with outbound dispatch initiated from handlers"), §6.11 Transport Reentry Contract ("Transports MUST support this reentrant dispatch pattern" — concurrent outbound, request_id demux tolerating out-of-order, per-request deadlines, no self-deadlock), §1.4 (a handler sub-request MAY target a remote namespace → routed outbound), and §9.0 ("the §9.1 floor — incl. request_id correlation — applies under **both** profiles"). What is *actually* extension-only is the validate-peer **`origination` test category** and the extension **consumers** (subscription/continuation/inbox). **Keystone misclassified the category-skip as a capability-scope ruling** — OCaml `transport.ml` header + OCaml/TS ambiguity logs assert "origination is extension-only under §9.0." Two real gaps: **(i)** that misclassification in our docs; **(ii)** machinery — **no peer exposes a handler-facing `hctx.Execute` outbound closure** (TS `PeerServices`/C# `HandlerContext` give tree/store/identity, not `execute`), and **OCaml has no client-side transport seam at all** (server-only). TS/C# already have the §6.11 transport correlation map (reader-loop + request_id pending-map, exercised — F23 `reference_connect`/`reference_ready` PASS), so the hard part exists; the missing universal piece is the handler-reachable closure. A core peer without it cannot host the extension handler classes (compute/relay/subscription) the substrate exists to support. | V7 §4.8/§6.11/§1.4/§9.0/§6.8; keystone `transport.ml`, TS `handler-abstractions.ts`, C# `HandlerContext` | `keystone` (peer build + doc correction) + `arch` (spec clarification, extend F23) | **Open — surfaced** (boundary research §4). **Decision:** (1) correct our docs; (2) build the handler-facing `execute` closure in all three peers (OCaml needs the client transport seam built first); (3) **ask arch** to add an explicit §9.0/§6.11 sentence that the outbound seam (reader-task + request_id correlation + `hctx.Execute`) is part of the core §9.1 floor even though no *core handler* originates — stops the next generated peer repeating the OCaml deferral. Extends **F23**. |
| **F26** | oracle boundary retroactive (no core-tier extensibility checks) | `validate-peer`'s `--profile core` is a **hand-maintained 14-category Go map** (`coreProfileCategories`, `profile.go`) carved retroactively out of a pre-existing full extension suite and refined reactively as the keystone surfaced gaps (the **F18** lineage — the oracle originally had no core-peer verdict). It now anchors to V7 v7.72 §9.0, but the anchor postdates the extensions and there is **no machine link** between V7 and the oracle's notion of "core" (drift risk). Consequence: the core gate **tests neither dynamic handler registration (F24) nor outbound origination (F25)** — a peer can be `--profile core` PASS while being a **responder-only** implementation that **cannot install a handler or originate a request**, i.e. is not actually an extensible substrate. | `validate-peer/profile.go` `coreProfileCategories`; `suite.go`; `handlers.go`; `origination.go` | `go` (oracle owner) + `arch` (boundary definition) | **Open — surfaced** (boundary research §5). **Ask go (+ arch sign-off):** add a **core-tier dynamic-register check** and a **core-tier minimal-origination check** (extension-free slice, with a reference peer) to `--profile core`; and re-derive the core boundary **machine-linked to V7** rather than a hand-maintained map. Keystone can co-specify the extension-free slices (we know exactly where the A-007 stubs and the outbound gap hide). The natural completion of F18: the gate proves *core conformance* but not *core extensibility*. |
| **F27** | spec-gap (the startup/owner-capability foundation, headline) | **No peer-owner / root / operator capability is established at startup in ANY of the three generated peers, and the model is not specified anywhere the generator can see.** Each peer holds an Ed25519 identity (signing key) but holds NO capability token authorizing itself. `--debug-open-grants` is the ONLY path by which the live peer reaches any write/grant-gated operation — and it does not mint a self/owner cap; it widens the grant minted **to whoever connects** from the §4.4 read-only floor to a `*`/`/*/*`/`*` wildcard (authority handed to an arriving remote, explicitly debug-only/non-conformant in all 3 peers). Without it, nothing can authorize a write. **Seed/preset capability policies don't exist:** `configure` writes `system/capability/policy/{pattern}` but NOTHING reads `system/capability/policy/*` during auth/connect (write-only, inert, 3-way). This breaks v7.74 v0.2's B1 framing: "the peer-owner cap satisfies the register cap-check vacuously" presupposes a peer-owner root cap that **the cohort each invented at peer-init (Go `core/peer/peer.go:209-211`) but V7 never mandates** — so the generator never emitted one. Three mature impls independently inventing the same bootstrap = the textbook signal it belongs in a spec. Load-bearing for B1/V2.0/L1 (register's cap-check has nothing to check against) and for the A5 substrate guarantee ("pop out a peer, immediately install a handler"). | C#/TS/OCaml `Peer` ctor + bootstrap; `ConnectHandler`/`connect_handler` `--debug-open-grants`; `system/capability/policy/*` (written-never-read); V7.74 v0.2 §6.1; SDK-OPERATIONS §2.1 | `arch` (where it lives: core/SDK) + `keystone` (cross-peer convention) | **Open — surfaced** by the 3-peer v7.74-v0.2 keystone review audit (§3). **Ask arch:** is the peer-owner root-cap bootstrap + seed-policy model specified anywhere? Where should it live (core §6.9-adjacent MUST/SHOULD / SDK-OPERATIONS §2.1 / nowhere)? Should `--debug-open-grants` retire for a principled `--owner-identity`/seed-policy mechanism? Is `system/capability/policy/*` a half-built read-path across the cohort too? **Keystone will own** the cross-peer startup-capability convention regardless (§8 of the review) so generated peers are consistent. **Blocks Phase B** (the register build). **Keystone first-principles design proposed** (the keystone peer-authority-bootstrap proposal): owner authority is a *capability, not a mode* (self-signed root); authenticate establishes identity, a declared identity→capability *seed policy* maps identity→scope, dispatch enforces; `--debug-open-grants` = the degenerate seed policy `default→*`. Reference design for arch to reconcile with the cohort's peer-init root-cap seeding. **RESOLVED — keystone signed off** (the V7.74 keystone sign-off handoff to arch). Arch reconciled the design as **Phase 2 proposal `PROPOSAL-V7-PEER-AUTHORITY-BOOTSTRAP` v0.5** (ratifies alongside v7.74): all 5 axioms preserved; all 5 §7 open questions answered the way keystone leaned (owner-default=self + `--operator` override; reuse `system/capability/policy/*`; eager declaration REQUIRED; no pre-auth authority; spec home SPLIT = V7 §6.9a invariant / SDK-OPERATIONS supply / keystone file-format). Arch additionally anchored on the v7.62 §8 union/subset + **v7.64 dual-form** (hex/Base58) policy substrate the cohort already ships (no new path mechanism), and added **§6.9a.0** naming two floor-conformant owner-cap shapes (detached-signature vs tree-as-trust-root; gate tests operational outcome not artifact placement). Cohort-implemented GREEN (Go/Rust/Py). Phase B unblocked on fold. |
| **F28** | cross-impl divergence (grant-signature placement) | V7.74 v0.2 §3.4 pins the grant-signature as a 5th `register` write **at `system/capability/grants/{pattern}/signature`** (claimed 3-way cohort convergence), and §10.1 step 4 hard-asserts it there. **Keystone peers diverge:** C# + TS write the grant signature at **`system/signature/{grant.contentHashHex}`** (the §3.5 invariant-pointer path) and the dispatcher verifies it there (`ValidateHandlerGrant`/`#validateHandlerGrant`) — functional + load-bearing, different path; **OCaml writes NO signature at all** (`mint_token` computes it, bootstrap discards via `let token, _ =`; bootstrap grant tokens carry empty grants + are never read). The v0.2 text is internally ambiguous: it cites "per V7 §3.5 invariant-pointer convention" yet places the write at a *child of the grant path*, while §3.5's actual pointer convention is `system/signature/{hash}` (what C#/TS do). As written, the §10.1 gate fails all three keystone peers. | V7.74 v0.2 §3.4/§10.1 vs §3.5 invariant-pointer; C#/TS `HandlerRegistry` + dispatcher; OCaml `peer.ml:553` | `arch` (path ruling) + `keystone` (OCaml build) | **Open — surfaced** (v7.74-v0.2 review §4). **Ask arch:** is the signature location normative-at-`grants/{pattern}/signature`, or is the §3.5 invariant-pointer (`system/signature/{hash}`) an acceptable conformant alternative (gate asserts "a verifiable grant signature exists + the dispatcher checks it", not a fixed path)? Keystone lean: contract = signature-exists-and-verified at the §3.5 pointer path (2 of 3 peers + the convention already use it). OCaml needs the write added regardless. **RESOLVED — keystone signed off.** v7.74 v0.4 §3.4 **converges on `system/signature/{grant_hash}`** — exactly the §3.5 invariant-pointer path keystone recommended. C#/TS unchanged; cohort (Go/Rust/Py) rename writer+reader (~10-20 LOC each); OCaml builds at the standard path in Phase B. §10.1 gate asserts the standard path + unregister teardown. The `grants/{pattern}/signature` colocation is retired. |
| **F29** | keystone test-coverage gap (shared wire corpus) | The pinned v0.8.0 ECF corpus (`shared/test-vectors/v0.8.0/conformance-vectors-v1.cbor`, 69 vectors) has **no vector exercising an array-of-maps whose inner map carries a text string ≥24 bytes** (the CBOR 1-byte-head → head+1-length boundary inside an array element). The only array-of-refs in the 53-type §9.5 floor is `union_of` in `system/capability/token`, so this shape is first reachable at **S4 type-registry publish**, not in the S2 codec corpus. **Consequence:** a codec can be **69/69-green yet carry a latent encoder bug** on this shape — as Forth #25 did (**A-FT-017**: the `emit-head` +1-byte case dropped a stack cell; hidden clean through S2, surfaced only when publishing the 53 types at S4). Any peer whose array/map encoder has an analogous +1-byte-head branch could carry the same latent defect and still pass the corpus (the "conformance-green can be vacuous" pattern, made concrete on the encoder). | `shared/test-vectors/v0.8.0`; RFC 8949 §3 head forms; Forth `A-FT-017` | `arch` (owns the corpus) | **Open — surfaced** by Forth #25 S4. **Ask arch:** add a covering vector — an array-of-maps with a ≥24-byte inner text value (ideally also a ≥256-byte one for the 2-byte-head boundary) — to the ECF corpus so the gap is closed cohort-wide; keystone can't edit the corpus (boundary). Not a spec defect; a test-surface gap. **HANDOFF-TO-ARCH drafted** 2026-07-12 → `protocol-generator/shared/findings/archive/F29-corpus-coverage-gap.md` (awaiting arch pull). Corroborated cohort-wide by **Smalltalk #26** (also 69/69-green on the same corpus at S2 — same blind spot, not Forth-specific). **RESOLVED 2026-07-12:** arch added the covering vectors `nested.5` (array-of-maps, ≥24-byte inner text, minor-24 head) + `nested.6` (256-byte inner text, minor-25 2-byte head) to the ECF corpus. Re-vendored here as `9695b1f1…` (71 vectors) from `entity-core-protocol` @ `be54baf`; byte pins decode-verified (F16 discipline). Cohort wire-conformance re-run **71/71 across all 27 codec peers** incl. the surfacers **Forth #25** (A-FT-017 fix holds) and **Smalltalk #26**. Gap closed cohort-wide. |
| **F23** | keystone-doc staleness (lifecycle vs profile) | The keystone's own `protocol-generator/shared/lifecycle/PHASE-S4-CONFORMANCE.md` gate table lists **`origination`** as an *extension-free category required for v0.1*. The authoritative v7.72 §9.0 oracle (`cb54f5b`) instead **auto-allowlists `origination` as "outside `--profile core` — extension-only category."** Confirmed by exercising it under the *full* profile with the Go `entity-peer` as `-reference-peer`: the core-reachable legs `reference_connect` + `reference_ready` **pass** (TS outbound dispatch works), while every failing check is extension over-demand (`async_202_A` = ASYNC §1 `deliver_to`/202; `rexec_put_b` + `xsub_setup_transport` = NETWORK §10 + a cross-peer cap the harness never grants). So origination is genuinely out of core scope, and the lifecycle doc's "required for v0.1 / extension-free" row is stale. The lifecycle table also predates the §9.0 `--profile core` profile entirely (it hand-lists 4 categories + a hand-exclude). | `protocol-generator/shared/lifecycle/PHASE-S4-CONFORMANCE.md` gate table vs `validate-peer --profile core` (v7.72 §9.0) | `research`/`operator` | **✅ RESOLVED** (TS peer #2 S4). **Fix applied:** `PHASE-S4-CONFORMANCE.md` gate section rewritten to define the gate as `validate-peer --profile core` (the §9.0 profile, F18) → clean `Result: PASS` (0 fail); `origination` reclassified extension-only (informative note on its outbound legs + the `-reference-peer` exercise); the stale hand-listed `-category`/`-exclude` invocation replaced with the §9.0-profile + in-container sealed-offline pattern (also fixes the latent bug that repeated `-category` doesn't accumulate in Go's flag parser). Keystone-internal, no V7 change. Companion to F18. |
| **F30** | corpus-defect (`.diag`↔`.cbor`, same class as F16) | The v0.8.0 ECF conformance corpus `conformance-vectors-v1.cbor` (SHA `41d68d2d…`) has 5 `tag_reject` vectors whose `.diag` each claim a CBOR **major-type-6 (tag)** item — but only **`tag_reject.4`** (`d9d9f7a0`, tag 55799) actually carries one. `tag_reject.1/2/3/5` contain **no mt6 item anywhere**: decoded, each is a leading `{type:"test/v1",data:"1"}`-shaped head + 8–40 bytes of **trailing garbage**, so a conforming decoder rejects them via the **full-consumption / trailing-data** rule, NOT the §6.3 tag scanner (confirmed in the reference C codec: `ecf.c` `if (r.pos != len) return NULL; /* trailing bytes */`). Consequence: a decoder with trailing-data rejection but **no tag scanner** still passes 5/5 `tag_reject` — N2 (mt6 reject) is effectively uncovered by the corpus (only `.4` exercises it). The "conformance-green can be vacuous" trap, in a corpus. | `test-vectors/v0.8.0/conformance-vectors-v1.cbor` `tag_reject.1/2/3/5` vs `.diag`; ENTITY-CBOR-ENCODING §6.3 (N2) | `arch` | **Open — surfaced** by the Fortran peer S2 (A-FTN-012). _(Renumbered **F29→F30** at the 2026-07-12 two-branch merge: the parallel branch independently assigned F29 to a **distinct** corpus finding — the array-of-maps ≥24-byte-head coverage gap — from the same pre-merge base; the Fortran S2 commit message references the pre-merge F29 number.)_ **NOT a peer bug** (the Fortran peer implements BOTH the recursive mt6 reject AND full-consumption → rejects all 5; and adds the REAL nested-tag coverage the corpus lacks in `test/unit_tests.f90 t_n2_tag_reject` — tag 0 top-level, tag 55799, tag nested in a map value). **NOT a spec-text defect** (§6.3 + N2 are correct). **Ask arch:** regenerate `tag_reject.1/2/3/5` from their (correct) `.diag` so the `.cbor` bytes actually carry the described tags (as valid-except-for-the-tag inputs), so N2 is corpus-gated for every peer — same fix shape + GUIDE-CONFORMANCE §3.2 decode-and-validate discipline as **F16**. Full write-up: `protocol-generator/shared/findings/archive/F30-tag-reject-corpus.md`. Does not block any phase. **RESOLVED 2026-07-12:** arch regenerated `tag_reject.1/2/3/5` so each is now **fully canonical (keys length-then-lex, `data` before `type`) EXCEPT the injected mt6 tag** — the old bytes had `type` before `data` and thus rejected on trailing-data, never gating the §6.3 tag scanner. Re-vendored here as `9695b1f1…` (71 vectors, SHA supersedes `41d68d2d…`) from `entity-core-protocol` @ `be54baf`; the 5 `tag_reject` frames decode-verified. Cohort re-run **71/71 across all 27 codec peers**, incl. the surfacer **Fortran (A-FTN-012)** whose unit suite confirms recursive tag rejection. N2 is now corpus-gated for every peer. |
| **F32** | spec-internal contradiction (§4.2/§4.4 body text vs §5.2a enumeration — auth-class status) | The v7.73 **§5.2a verdict-to-status enumeration** (normative; line ~2263) explicitly routes a request-time **`Author absent`** failure to **401 `authentication_failed` (auth-class)** — the envelope has no verified signer, so it is authentication, not authorization. But two older spec-body statements still mandate **403** for the same missing-`author` case: **§4.2 bullet 3** ("EXECUTE targeting any other path without auth fields MUST be rejected (status 403)", line ~1593) and the §4.4 request-auth statement ("Every authenticated EXECUTE MUST include `author` and `capability`… Peers MUST reject requests missing **either** with status 403", line ~1893). The "missing **either** → 403" is half-wrong under §5.2a: **capability-absent stays 403 (authz)** (§5.2a row confirms) but **author-absent is 401 (auth-class)** — the two auth fields split across the auth/authz boundary, and the older body text predates that split (it was not back-propagated when §5.2a codified the discriminator in v7.73). Consequence: an impl reading only §4.2/§4.4 emits 403 for a no-author EXECUTE; an impl reading §5.2a emits 401 — a **cross-impl status divergence on a MUST-emit tuple** that clients key error handling off. Directly in the **F20** lineage (F20 corrected the *oracle/memo* side to auth-class-401; F32 names the *spec-body* text F20's fix implies but did not touch). | ENTITY-CORE-PROTOCOL.md §4.2 bullet 3 (~L1593) + §4.4 (~L1893) vs §5.2a enumeration row `Author absent → 401` (~L2263); §5.2a discriminator (~L2256); related **F20**, **F31** (§6.5 auth-before-resolve) | `arch` (spec-body reconciliation) | **Open — surfaced** by the **Nim** peer S3 (A-NIM-009), independently corroborated by the **Julia** peer (same auth-before-resolve trichotomy 401/403/404). **NOT a peer bug** — both peers followed **§5.2a (401)**, the newer, load-bearing, conformance-vector-pinned tuple, consistent with F31's §6.5 auth-before-resolve invariant and F20's auth-class-401 disposition. **Ask arch:** reconcile the §4.2 bullet 3 + §4.4 body text with the §5.2a enumeration — a no-/bad-`author` (or absent/invalid signature) EXECUTE on a non-`connect` path is **auth-class → 401**, while capability-absent remains authz-class → 403; the blanket "without auth fields → 403" and "missing either → 403" phrasings should be split accordingly (or explicitly cross-reference §5.2a as authoritative-on-the-tuple). Not a spec *logic* defect (§5.2a is correct); a documentation-reconciliation debt left by the v7.73 amendment. **Corroboration confirmed:** both peers `validate-peer --profile core` PASS **682·0F @ cc1970f** (Nim 293P/293W/0F/96S; Julia 292P/294W/0F/96S), both follow §5.2a→401 and pass the oracle `security` category (which pins auth rows at 401) — the 401 reading is oracle-conformant. **HANDOFF-TO-ARCH drafted** → `protocol-generator/shared/findings/F32-auth-class-status.md` (awaiting arch pull). |
| **F33** | probe-vs-spec-intent tension (§6.11 T2.1 completeness assertion) | Surfaced by the **wasm-wat** peer reaching `--profile core` green (2026-07-15). §4.9 is explicit that §6.11 robustness is an **"outcome contract, not a performance bar … slow peer that degrades gracefully is conformant … per-language-fair, not by cross-language absolute numbers"** (~L1860), and back-pressure ("block or slow the producer", ~L1858) is a sanctioned mechanism. T2.1's *latency* gates honor this (all ratio-based: `lastP50/firstP50 > 4.0`, `concurrency.go:460-462`). But its **completeness** gate (`dropped>0 → FAIL`, `concurrency.go:445-453`) counts the validator's **own** client-side `read response: i/o timeout` as a drop — from a fixed `perRequestTimeout=20 s` (`client.go:198`) AND the shared `-timeout=60 s` run budget (`main.go:36`, `ioDeadline` returns the earlier, `client.go:209-215`). For a slow serial peer the **60 s aggregate budget binds** (10 000×~18 ms crypto ≫ 60 s), collapsing per-request deadlines mid-run → mass "drops" (we saw 7002/10000). This conflates §4.9(c) *silent drop* (peer discards, no response ever — non-conformant) with *late-but-progressing* (a conformant graceful slowdown). A lawfully-**shedding** peer (coded refusal) would also surface `err != nil` and FAIL. Net: a fixed `16×10 000` load + fixed client deadline = an **absolute aggregate-throughput floor of the client's making**, in tension with the "not-a-performance-bar / per-language-fair" text. Also: the shared 60 s budget has **no per-category sub-budget** (`suite.go:185-214`, the `budget_exhausted` skip), so a slow-but-passing category starves later categories — pass/fail is run-order-sensitive. | ENTITY-CORE-PROTOCOL.md §4.9 (~L1854, ~L1860), §6.11(c) (~L3718); GUIDE-CONFORMANCE §7b T2.1/T2.2; oracle `cmd/internal/validate/{concurrency.go:89-99,445-462; client.go:198,209-215,432-443; suite.go:185-214}` @ `cc1970f` | `arch` (probe design + spec clarity) | **Open — surfaced.** **NOT a peer bug** — wasm-wat is GREEN (`--enable-jit` → verify ~9 ms→84 µs, full per-request §5.2 verify passes with margin). **Ask arch:** (1) distinguish late-vs-lost — drain to completion under a generous/adaptive deadline, assert every admitted request gets a response-or-coded-signal (the real §4.9(c) property), keep the ratio gate for latency-runaway; and/or (2) model a back-pressurable producer (client slows when peer slows, §4.9's sanctioned mechanism); and/or (3) document the pragmatic floor + that slow substrates MUST run a compiled execution mode (make it an explicit §6.11 conformance-contract element); and (4) add a per-category `-timeout` sub-budget to de-flake run-order sensitivity. Written up in `protocol-generator/shared/findings/concurrency-latency-floor-and-cap-sig-coverage.md` (awaiting arch pull). |
| **F34** | oracle **coverage gap** (no tampered-cap-signature vector) | Surfaced alongside F33 while auditing whether a granter-sig-verdict cache is sound. `security.tampered_signature` (`security.go:569-597`) flips the **author** signature's detached bytes on a **warm** connection → 401 (the guardrail that makes per-request author verification non-negotiable; it caught exactly a per-connection author-verdict cache in wasm-wat during dev). There is **no symmetric probe** that flips the **granter/capability** signature's detached bytes over an *unchanged* cap `content_hash` on a warm connection. Audit of the tampering probes: `grantee_author_mismatch` (`security.go:600-670`) leaves the cap untouched; `forged_root_capability` (`security.go:672-736`) builds a **new** cap → new hash; F6 `content_hash_substitution` (`security_chain.go:461-462`) mutates cap **DATA** → recomputed hash differs (and uses a *fresh* conn). Every stale-cap-verdict path either changes the cap hash (hash-keyed cache legitimately misses) or tampers a *different* signature. **Consequence:** a peer memoizing "granter sig over cap-hash H is valid" keyed on **H alone** passes ALL security/authz checks while a later warm-conn request re-presenting H with garbage granter-sig bytes is **wrongly admitted** — the exact class `tampered_signature` prevents for the author sig, with **zero coverage** for the cap sig. | oracle `cmd/internal/validate/{security.go:569-736; security_chain.go:387-470}` @ `cc1970f`; ENTITY-CORE-PROTOCOL.md §4.10(b) (~L1867), §5.2/§5.5 | `arch` (oracle coverage) | **Open — surfaced.** **Ask arch:** add `tampered_capability_signature` (warm conn, valid cap, corrupted **granter** sig bytes → **403 capability_denied**), symmetric to `tampered_signature`. **Bonus (sanctioned optimization):** the granter-sig verify IS soundly memoizable if keyed on **(cap-hash, detached-signature-bytes)** — a pure `memoize-verify-by-(message,signature)` cache (tamper → miss → reject; honest-repeat → hit) — giving slow-substrate peers ~2× per-request crypto headroom for repeated-cap loads (§4.10(b) acknowledges the O(depth) verify cost), no §5.2 weakening. The *author* sig is over a per-request-unique message so it never hits (stays per-request, per F34's guardrail). Arch may wish to note this pattern in the spec/GUIDE. Written up in `protocol-generator/shared/findings/concurrency-latency-floor-and-cap-sig-coverage.md` (awaiting arch pull). |
| **F35** | oracle **coverage gap** (§7a reentry-echo skips §5.2 → outbound-authorization dimension untested) | Surfaced while building the wasm-wat `system/validate/dispatch-outbound` reentrant dialer (the last skip-to-pass to true cohort parity). §7a.1 specifies dispatch-outbound as *"originate ONE outbound EXECUTE via the §6.11 reentry seam"* carrying the reentry authority (`WithCapability(cap)` + `WithIncludedChain([granter, sig])` — `ext/conformance/handler.go` `DispatchOutboundHandler.Handle`). But the **validator-side** counterpart that receives that outbound EXECUTE — the armed reentry-echo `handleReentryEcho` — runs **zero verification**: `ExecuteDataFromEntity(env.Root)` (parse only), match `ExtractHandlerPath(uri)==system/validate/echo && operation==echo`, `hits++`, respond `Result=execData.Params` verbatim. No §5.2 author-sig verify, no capability-chain verify, no §5.5 granter check. **Consequence:** a peer whose dispatch-outbound emits an **unsigned, capability-less** outbound EXECUTE (just `{uri, operation:"echo", params, request_id}`, no author/capability/included) still increments `hits` and round-trips → **t1_1 `validate_echo_dispatch` + t1_2 `t1_2_concurrent_reentry` PASS**. The §7a probe therefore does **not** gate the one property the handler exists to prove — that the peer can originate an *authorized* outbound EXECUTE through the §6.13(b)/§6.11 seam (the F25 substrate guarantee). Vacuous-green on the outbound-authorization axis; symmetric to F34 (inbound cap-sig tampering) but on the origination surface. | oracle `cmd/internal/validate/conformance_handlers.go` (`handleReentryEcho` ~L84-140; `ArmReentryEcho`) @ `cc1970f`; `ext/conformance/handler.go` (`DispatchOutboundHandler`, `WithCapability`/`WithIncludedChain`); GUIDE-CONFORMANCE §7a.1/§7a.2a; ENTITY-CORE-PROTOCOL §6.13(b)/§6.11; related **F25** (outbound seam is core-MUST), **F34** | `arch` (probe design) | **Open — surfaced.** **NOT a peer bug** — wasm-wat implements the **full authorized** outbound envelope (author-signed root + `capability` + `included{granter, cap-sig, author-sig}`), the spec-faithful reading, *not* the minimal unauthorized frame the probe would accept (building the minimal one would itself be the vacuous shortcut the keystone exists to catch). **Ask arch:** have the reentry-echo (or a dedicated sibling probe) run the §5.2 verify ladder on the inbound reentrant EXECUTE (author-sig over root, cap-chain grantee=peer, granter-sig over cap) and only then echo — so §7a actually gates that dispatch-outbound authored an *authorized* EXECUTE. Optionally add a negative arm (peer originates an unsigned/wrong-grantee outbound → validator rejects → probe FAILs) to lock the authorization contract. **HANDOFF-TO-ARCH drafted** → `protocol-generator/shared/findings/wasm-dialer-parity-F35-and-execution-mode.md` (companion to the F33/F34 handoff; awaiting arch pull). Corroborated by construction: wasm-wat ships the full authorized envelope and passes; the minimal unsigned frame would pass the identical probe. |
| **F36** | naming/documentation trap (two "signature" constructions) | Surfaced by the **asm-x86_64 L2 native-codec differential** (2026-07-15). The corpus `signature` category signs the target entity's **canonical ECF bytes** — `ed25519_sign(seed, ECF({type,data}))` — **empirically pinned**: the golden 64-byte sigs match signing the ECF bytes and do **not** match signing the 33-byte content_hash (format code + digest) nor the 32-byte digest; sign/verify round-trips independently, isolating it to *message choice*. `signature.2`'s unsorted data `{z:1,a:2}` further confirms the signed message is the *canonically-sorted* ECF (native sorting `ec_encode_ecf` reproduces the golden; unsorted input does not). This is a **different construction** from the §7.3 protocol signature over the **content_hash** ("Sign full hash bytes: format code + digest") for authenticate/capability entities. Both are legitimate — the corpus tests the lower-level canonical-encode-then-sign codec primitive; §7.3 wraps it over the content_hash — but an implementer reading only §7.3 and assuming "signatures sign the content_hash" **fails the corpus `signature` vectors** with no spec pointer to the distinction. | `ENTITY-CORE-PROTOCOL.md` §7.3; `ENTITY-CBOR-ENCODING.md` §E `signature.*` @ `spec-data/v0.8.0`; local **A-ASM-018** | `arch` (spec clarity) | **Open — surfaced.** **NOT a defect** — both constructions are correct; the asm-L2 codec passes `signature.1/2/3` via native sorted `ec_encode_ecf` + FFI `ec_ed25519_sign` (`make diff` green, symbol-verified). Same "conformance-green can hide a real distinction" family as F34/F35. **Ask arch:** a one-line note wherever the corpus `signature` vectors are described, distinguishing the **ECF-signature** (codec/corpus: sign the canonical ECF of `{type,data}`) from the **content_hash signature** (§7.3 protocol) — naming the two removes the trap. **HANDOFF-TO-ARCH drafted** → `protocol-generator/shared/findings/asm-018-signature-construction.md` (awaiting arch pull). |
| **F37** | spec-internal contradiction (§4.8 bootstrap name vs §10.1/appendix + oracle — peer-identity type name) | The peer-identity address primitive (`primitive/string`-extending, Base58 peer id) is named **inconsistently within ENTITY-NATIVE-TYPE-SYSTEM.md v0.8.0**: the **bootstrap definition** sections call it **`system/identity/peer-id`** — §1 intro (L19), §4.4 bootstrap table row 14 (L409), §4.8 heading + body (L508–526), §2.7 four-address-primitives rationale — and it is used as a `type_ref` at **L639** (`system/peer` example) and **L1082** (`system/protocol/connect/hello`). But **§10.1 `system/peer`** (L1453) and **Appendix B** (L2399) reference **`system/peer-id`** (no `identity/`) as the `type_ref` for the *same* concept. The **reference oracle's ratified-core type set carries only `system/peer-id`** (fetch/match probes at `system/type/system/peer-id`; there is **no** `system/identity/peer-id` probe). So **both names are live `type_ref`s in the same spec version**, and the reference impl settled on the shorter one — a peer registering only the §4.8 bootstrap name leaves the §10.1/hello `type_ref`s dangling (and 404s the oracle's `type_system_peer_id_fetch`). Same class as **F32** (spec-body text not back-propagated across an amendment), but on a *type name* rather than a status code. **Secondary (same table):** §4.4 says "The complete set of **14** bootstrap types" but the table lists **15 rows** (row 15 = `entity`, §3.1.1) — an off-by-one in the count vs the enumerated set. | ENTITY-NATIVE-TYPE-SYSTEM.md §4.4 (L409) / §4.8 (L508–526) / L19 / L639 / L1082 (`system/identity/peer-id`) vs §10.1 (L1453) / Appendix B (L2399) (`system/peer-id`); oracle `validate-peer` ratified-core `type_system` set (`system/peer-id`, no `identity/`); §4.4 "14" vs 15-row table | `arch` (spec-body reconciliation) | **Open — surfaced** by the **Pure Data** peer S3.7 (A-PD-012), corroborated by the oracle's ratified-core set. **NOT a peer bug** — the Pd peer follows the spec by registering the §4.8 bootstrap name **`system/identity/peer-id`** AND also binds **`system/peer-id`** (the §10.1/appendix + oracle name) so **every** spec `type_ref` resolves; both render identically (`{name, extends:"primitive/string"}` over `system/type`) and green the oracle's `type_system_peer_id_fetch`/`_match`. **Ask arch:** pick ONE canonical name for the peer-identity primitive and reconcile all `type_ref`s + the §4.4 bootstrap table to it (and fix the "14"→15 count); if both names must coexist, say so normatively (alias). Not a spec *logic* defect; a naming/count reconciliation debt. **Update (Pd S3.8, full core type floor):** the peer-id split is one facet of a **systemic staleness of ENTITY-NATIVE-TYPE-SYSTEM.md Appendix B vs the primary ENTITY-CORE-PROTOCOL.md `:=` definitions** (the oracle is built from the primary doc): Appendix B renders `core/envelope.root` / `system/protocol/execute.params` / `.../execute/response.result` as **`primitive/any`** where the primary doc (§3.1/§3.2/§3.3) + oracle use **`core/entity`**; renders `connect/hello.peer_id` / `connect/authenticate.peer_id` as **`system/identity/peer-id`** where the primary doc + oracle use **`system/peer-id`** (the oracle never uses `identity/peer-id` anywhere); spells out `system/envelope` as duplicated `{root, included}` fields where the primary doc has it **extend `core/envelope`**; and omits that `system/handler/manifest` **extends `system/handler/interface`** (§3.7). **Ask arch:** regenerate Appendix B from the primary `:=` definitions so the type-system doc's consolidated reference stops disagreeing with the normative source (same doc-reconciliation shape as the F37 peer-id split). The Pd peer authored from the primary doc → all core `type_system` `_fetch`+`_match` green under `--profile core`. **HANDOFF-TO-ARCH drafted** 2026-07-15 → `protocol-generator/shared/findings/F37-peer-id-naming-appendix-b.md` (awaiting arch pull). |
| **F31** | keystone-internal **stale unit tests** (two distinct causes), cohort-scoped | Surfaced incidentally during the F29/F30 cohort sweep (2026-07-12): 4 peers (elixir, csharp, cobol, typescript) failed a peer-layer unit test while their **ECF codec gate was 71/71 green** and they **pass validate-peer S4** (the authoritative gate). Bisected to **two distinct stale-TEST causes, both test-side — the peer implementations were spec-correct throughout:** **(A) §7a dispatch-outbound reentry (elixir/csharp/typescript):** the unit tests sent the dispatch params' `value` field as a **bare scalar** (`"round-trip-99"`), but the §7a.1 contract — per the Go oracle `conformance_handlers.go` (`ecf.Encode({"value": value})`, then `NewEntity("primitive/any", …)` pass-through) and the passing kotlin test (sends a `{ping: …}` **map**) — is that `value` **is the outbound params entity DATA**, i.e. the `{value: X}` echo-shape, forwarded verbatim by the (correct) pass-through handler. A scalar has no `value` field for echo to reflect → the round-trip assertion `result.value == X` failed. Handlers were already oracle-conformant; **wrapping would double-wrap and BREAK the oracle** (the elixir handler comment already documented this). **(B) cobol `dispatch-test`:** a "dispatch skeleton" test sent an **unauthenticated** EXECUTE (no author/signature) to an unknown handler and expected **404**; but per §6.5 `verify_request` (authentication) runs **before** handler resolution (ENTITY-CORE-PROTOCOL.md §6.5, verify_ctx "constructed … before handler resolution"), so an unauthored request correctly returns **401** before the 404 path is reachable. cobol was spec-correct (and S4-conformant); the skeleton test predated the auth chain. | Go oracle `cmd/internal/validate/conformance_handlers.go` (`SendDispatchOutboundProbe`); ENTITY-CORE-PROTOCOL.md §6.5 (line ~2204 verify-before-resolve, ~3461 resolution-first-for-authz); tests: elixir `peer_test.exs`, csharp `ConformanceHandlersTests.cs`, ts `conformance-handlers.test.ts`, cobol `dispatch-test.cob` | `keystone`/`operator` (peer tests) | **✅ RESOLVED 2026-07-12.** (A) corrected the 3 reentry tests to send `value` as the `{value:X}` map (matching the Go oracle + kotlin) → elixir 28/28, csharp 34/34, typescript 62/62. (B) corrected the cobol skeleton test to assert **401 authentication_failed** (the spec-correct terminal status for an unauthenticated EXECUTE; 404 handler_not_found is exercised under authenticated dispatch by validate-peer S4) → cobol full suite PASS. **No handler/peer code changed — all four were spec-correct; only stale tests were fixed.** Kotlin/Go were the reference-correct diff targets throughout. |
| **F38** | guidance gap (content-addressed mint aliasing) | **Mint-timestamp precision is a CORRECTNESS parameter, not formatting.** A capability token is content-addressed over `{grants, grantee, granter, created_at}`; a peer stamping `created_at` at **second** precision makes same-scope same-grantee re-mints within one second **byte-identical → same content hash → the same capability**. The oracle's `revoke_happy_path` re-mint then aliased the §4.4 **session floor cap** — revoking one revoked the session, and every later category 403-cascaded. Nastiest property: **isolated `-category` runs stay green; only the full-profile marathon exposes it** (capability runs early on a long-lived session). No spec text marks the precision as load-bearing; no vector gates it directly — the C peer's `ec_now_ms()` avoided it *implicitly*, so cohort greenness here is accidental, not spec-driven. | ENTITY-CORE-PROTOCOL.md §3.6/§5.5 token shape; GUIDE-CONFORMANCE (no note) | `arch` (GUIDE-CONFORMANCE note) | **Open — surfaced** by the **Pure Data** peer #33 (A-PD-016) at the full-gate marathon. **Keystone fixed:** ms-precision wall clock (`clock_gettime(CLOCK_REALTIME)`) at every mint site → Pd **682·0F Result: PASS** stable ×4; durable lesson pinned in AGENTS.md. **Ask arch:** GUIDE-CONFORMANCE note — `created_at` MUST be ms-precision from a real-time clock at every mint site (or mints otherwise distinguishable); a direct vector is timing-dependent, the note is the vehicle (F29 "green-can-be-vacuous" family). **HANDOFF-TO-ARCH drafted** 2026-07-15 → `protocol-generator/shared/findings/F38-mint-timestamp-precision.md` (carries F39 as companion; awaiting arch pull). |
| **F39** | guidance gap (§5.5a consequence, open-seed form) | **An open/debug seed with bare `resources: ["*"]` cannot cover foreign namespaces** — §5.5a makes bare `*` **granter-local** (`/{granter}/*`), so the `universal_address_space` probes' writes into `/{fixturePeer}/…` have no covering grant and the category **silently SKIPs** ("connection grants do not cover") — a skip, not a fail, easy to miss. The seed must carry BOTH forms: `resources: {include: ["*", "/*/*"]}` (what the Go `-open-access` peer does internally). | V7 §5.5a bare-star ruling; `validate-peer` `universal_address_space` skip path | `arch` (one-sentence guide note; optional skip-message diagnosability) + `research` (convention doc — done) | **Mostly resolved keystone-side — surfaced** by the **Pure Data** peer #33 (A-PD-017). Pd's `EC_OPEN_GRANTS` seed fixed; the keystone-owned `protocol-generator/shared/seed-policy/README.md` degenerate-open form already documents `{resources:["*","/*/*"]}`; AGENTS.md lesson pinned. **Residual arch ask (small):** a GUIDE-CONFORMANCE/SDK-OPERATIONS §2.1 sentence on the dual form + optionally name the missing absolute form in the oracle's skip message (currently reads environmental, not seed-shape). Folded into the **F38 handoff** (companion section). |
| **F55** | guidance gap — §1.5's `Status` column has no stated normative force | **WITHDRAWN AS FILED, AND THE WITHDRAWAL IS THE FINDING.** First filed as an oracle defect: `put_unsupported_content_hash_format_400` drives format code **`0x02`** and its source comment calls it *"unallocated"*, while **§1.5 allocates it** (ECFv1-SHA-512, 64 B digest, 65 B total, status **Reserved**). The comment is still inaccurate; **the accusation was not.** Checked before sending, per the standing rule: go's `core/hash.DigestLen` implements **`0x00` and `0x01` only** (`0x02` → 0 → `ErrUnknownAlgorithm` at *decode*), and go's own `2026-09-06-b` report records the row **three-way green across go, rust and py**. **All three ground-up implementations read `Reserved` as not-implemented. Our keystone `go` peer was the outlier** — it verified `0x02` on the reasoning, written in its own comment, that an allocated code with a known algorithm is computable and so should verify. Both readings are defensible **because §1.5 never says what its Status column obliges**: `Production` / `Validated` / `Reserved` / `Allocated; validation deferred` carry no MUST, MAY or SHOULD. The new wire vector is the first thing to make the difference conformance-visible. | `ENTITY-CORE-PROTOCOL.md` **0.8.2.11** §1.5 (format-code table + Status column), §4.5a item 2; searched before filing: `entity-core-go` `core/hash/hash.go` `DigestLen`, `docs/validation/reports/2026-09-06-b-*`, `docs/validation/spec-issues/2026-09-01-b-*`; `entity-system-architecture` `PROPOSAL-THE-PUT-ADMISSION-PREDICATE-AND-THE-CONTENT-CODE-TABLES.md`, `COHORT-OPEN-ITEMS.md`, `ROUTING-2026-09-06-c`, `ROUTING-2026-09-01-c` | `arch` (Status-column force) + `go` (comment accuracy, cosmetic) | **Open — reduced** 2026-09-07. **Our peer is FIXED, not the vector**: keystone `go` now verifies `0x00`/`0x01` only, on the three-way convergence and NOT on the vector — aligning to a vector is authoring against the oracle, aligning to three independent implementations is not. **Ask (small):** give §1.5's Status column a stated force, so *Reserved* answers "may a peer verify this?" without three implementations having to converge by accident. **Cosmetic:** the vector's `// format 0x02 (unallocated)` comment should read *reserved, unimplemented by all three seats*. |
| **F56** | oracle **internal contradiction** — two checks in one binary demand incompatible behaviour | `connect_incompatible_protocol` (new, core `connectivity`) requires a peer to refuse a hello whose non-empty `protocols` set does not intersect its own — **`400 incompatible_protocol`**, per 0.8.2.4 making the intersection normative. `agility_unknown_1` (`format_agility.go:510`) then sends a hello carrying **`Protocols: ["entity-core/v7"]`** and requires `400 unsupported_key_type`. `entity-core/v7` is a **spec-line name, not a §8.4 protocol version identifier** (today the single value `entity-core/1.0`) — so that hello **is** protocol-disjoint, and a peer satisfying the first check refuses it on protocols before ever reaching the key_type surface. **§4.5 states no precedence among the three negotiated fields**, so the check's result depends on an unruled ordering: refuse `key_types` first and it PASSes, refuse `protocols` first and it FAILs, both conformant. This is precisely the failure §4.5 predicts for this value — *"a peer advertising an identifier that resembles this document's section numbering rather than §8.4's protocol version is well-formed, silently non-interoperable, and **undetectable until the intersection is first enforced**"* — surfacing the first time it was enforced. | `ENTITY-CORE-PROTOCOL.md` **0.8.2.11** §4.5 (`protocols` row + the §8.4-identifier paragraph), §4.7 rows 1 and 4, §8.4 | `arch` (precedence) + `go` (probe identifier) | **Open — surfaced** 2026-09-07. **Two asks, and the second is the durable one:** (a) correct the probe's hello to `entity-core/1.0`; (b) rule whether §4.5 fixes a precedence among `hash_formats` / `key_types` / `protocols`, or state explicitly that it does not and that no check may depend on one. Our peers order `protocols` LAST, matching the reference. |
| **F57** | oracle **coverage gap at the corner of a two-variable rule** | §4.7's **0.8.2.6** note is a table over connection *state* × *address*, and it pins the cell the prose exists for: a **pre-establishment** EXECUTE naming a **foreign** namespace is **`400 invalid_request`**, not the `401 authentication_failed` the own-namespace row takes — because *"a `401` directs the caller to authenticate and retry, and for a foreign-namespace address that retry cannot succeed at any authentication state."* The oracle ships `execute_before_established_refused` (pre-establishment × **own**) and `dispatch_inbound_foreign_namespace_refused` (**established** × foreign). **A peer that evaluates authentication first passes both and fails the corner**, which no vector supplies. Measured on the wire across the roster: **37 of 46 answer 401** (`sql`, 403), **9 answer 400** — so the row is implementable and the reading is not in doubt. Every failing peer's **differential control** (same URI, established, authenticated) answers `400 invalid_request`, which is what makes this a statement about evaluation order and not about our URI. Found by the first **Kind C independent check** (`docs/VERIFICATION-ARCHITECTURE.md`), authored from the spec text rather than from the oracle's source. | `ENTITY-CORE-PROTOCOL.md` **0.8.2.11** §4.7 (the 0.8.2.6 address table + its 0.8.2.5 predecessor note), §1.4 (inbound dispatch MUST), §5.2a, §6.5 step 3; searched for the absence, by name: the candidate oracle's full declared-check inventory and `go`'s own 778-check executed set, every check matching `foreign_namespace`, `prehello`, `preestablish`, `establish`, `connect_*` | `go` (one vector) — the **peer** work is keystone's | **Open — surfaced** 2026-09-07; **peer side CLOSED at 46 of 46** 2026-09-08. **Ask (one check), unchanged:** a `catConnectivity` vector on the intersection. **What changed is the evidence and it cuts both ways, so here is all of it:** all 46 keystone peers now answer `400 invalid_request`, each verified by the Kind C check with its differential control — but **four INDEPENDENT lineages answered `401` before we changed ours** (`entity-core-go`, `entity-core-rust`, `entity-core-py`, and our own `go` peer). That is genuine independent convergence on the opposite side of a table that names the status, the code and its own reason in one paragraph, with §1.4 supplying the MUST. We implemented the SPEC on this repo's standing boundary (derive behaviour from the spec, not from the oracle) and are flagging the disagreement rather than treating our reading as settled; **reversal cost is stated so the decision stays cheap — 25 peers × a three-line hoist.** The thing not to do is leave it unruled: four implementations answering `401` against explicit text is the shape that produces a silent interop split. Full write-up: [`connect-error-table-independent-check.md`](../../protocol-generator/shared/findings/connect-error-table-independent-check.md), which also records **two divergences that were drafted as a second gap and WITHDRAWN** — the candidate oracle catches both minted codes (`501 operation_not_supported`, `401 missing_author`) by name, so they are cohort debt and corroboration, not an ask. |


### 2026-07-27 — 0.8.1 bucket-B applied to the cohort (F47/F48 family; RT-6/RT-13/RT-14)

Full write-up: [`bucket-B-cohort-application.md`](../../protocol-generator/shared/findings/bucket-B-cohort-application.md).
Two net-new findings, both routed to `arch`:

| ID | Kind | Disposition |
|---|---|---|
| **F49** | spec-gap — normative pseudocode contradicts the pinned prose | The F40 id-scope pin (`entity-core-protocol` `6285c94`) landed in §5.2's prose, the scope-type paragraph, and the `operations` table row — but **not** in the `matches_scope` code block, which still canonicalizes every dimension, nor in §5.5a `scope_subset`. **Peers are generated from those blocks**, so a regeneration at `c32d2c5` reproduces F40. → `arch`: amend the pseudocode to take the scope type. **Open.** |
| **F50** | scope question — does F40 reach `scope_subset`? | §5.5a canonicalizes `operations`/`peers` in the delegation containment test. F40 names `matches_scope` only. `scope_subset` is pattern-vs-pattern, not value-vs-pattern, so extending "compared as literal identifiers" to it is a larger change than F40 made and alters which delegations are accepted. **Every converted peer holds the existing behaviour pending arch's ruling** — guessing is how the cohort re-splits. **Open.** |
| **F54** | spec-internal contradiction (§4.6 pseudocode vs §3.5 normative `MUST NOT`) | `ENTITY-CORE-PROTOCOL.md` **0.8.2.11** (snapshot `c97e1860…`) §4.6's proof-of-possession pseudocode reads `data: { peer_id, public_key, key_type }` (L1855) then `peer_hash = content_hash(peer_entity)` (L1857). §3.5 of the **same file** declares `system/peer := { public_key, key_type }` with *"peer_id MUST NOT appear in the system/peer entity's hashable basis"* (v7.65), and the §1 summary agrees (`; v7.65: peer_id exits hashable basis`). **§4.6 is the outlier, and it is the section an implementer reads while writing the handshake.** Same class as F32/F37 — body text not back-propagated across an amendment — but on the *hashable basis* rather than a status code or a type name. **Consequence:** an implementer following §4.6 literally builds a three-field `system/peer`, and every conformant peer answers `401 unresolvable_grantee` — an error naming the grantee, not the basis. | `ENTITY-CORE-PROTOCOL.md` §4.6 (L1855, L1857) vs §3.5 (`system/peer :=`, ~L890) and the §1 summary (L62), `spec-data/v0.8.2.11/` | `arch` (spec-body reconciliation) | **Open — surfaced** 2026-09-07 while building `tools/put-probe`, which was written against the pseudocode and was refused by all 46 peers. **NOT a peer bug** — the cohort follows §3.5. **The probe's positive control is the only reason this did not publish as a cohort-wide defect on 46 peers**, which is the p47 no-uncontrolled-probe rule paying out on its second instrument. **Ask arch:** correct §4.6's pseudocode to the two-field form, or mark it explicitly informative and cross-reference §3.5. Routed to arch **2026-09-07**, §2 of that date's escalation packet — which is an in-flight process artifact and does not publish, so it is named by date rather than by path. |
| **F58** | spec gap — **no precedence between a NEGOTIATION refusal and a CONNECTION-STATE refusal**, and the choice is observable | F56's sibling: F56 asks for a precedence AMONG the three negotiated fields; this asks for one BETWEEN the §4.5 negotiation refusals as a group and §4.7's rows 8–9. A second `hello` carrying a disjoint `hash_formats` on a **half-open** connection satisfies both at once — §4.5/§4.7 row 2 → `400 incompatible_hash_format` (the CONTENT is unacceptable) and §4.7's out-of-order row → `409 connection_sequence_error` (a connect operation the responder implements, arriving in a state that forbids it). Both correct in isolation; neither section orders them. **Measured, not hypothetical:** on `prolog` in a full `--profile core` run `negotiation/format_disjoint_reject`'s hello arrives half-open — traced at the peer, `established=false, hash_formats=["ecfv1-fake-disjoint-format"]` — so a state-first ladder answers 409 where the check wants 400, while `-category negotiation` alone delivers the same hello on a FRESH connection and either order passes. **A conformant peer can fail this check for a reason no reading of its source reveals, depending on what ran before it.** Content-before-state satisfies both vectors and is what our peers do where it was measured to matter; on most peers the vector arrives fresh and the choice is invisible. | `ENTITY-CORE-PROTOCOL.md` **0.8.2.11** §4.5 (the three negotiated fields), §4.7 rows 2 and 8–9 + the 0.8.2.4 state-conflict-vs-unknown-op note; oracle `negotiation/format_disjoint_reject` | `arch` (precedence) + `go` (connection state the check controls) | **Open — surfaced** 2026-09-07, registered 2026-09-08. **Ask (either, but not silence):** rule a precedence — we suggest content before state, since negotiation is a property of the FRAME and the state check a property of the CONNECTION, and it satisfies both existing vectors — or state that none is fixed, in which case no check may depend on one and `format_disjoint_reject` needs a connection whose state it controls. Routed to arch **2026-09-08**, §2 of that date's escalation packet — which is an in-flight process artifact and does not publish, so it is named by date rather than by path. |
| **F59** | oracle gate defect — a not-drivable SKIP outside the §9.0 carve-out makes the documented entry point report `FAIL` and exit non-zero **on a 0-FAIL peer, including `go`** | At the candidate check set (go `78db4a9`, 778 executed) `connectivity/connect_ping_before_hello` SKIPs on any peer that does not serve the NETWORK-extension `ping`. The skip is **not** in the §9.0 profile carve-out, so a `--profile core` run prints *106 skip(s) auto-allowlisted … 1 skip(s) count as FAIL* and ends `Result: FAIL (un-allowlisted skips)` with a JSON summary of `{"failed": 0}`. **The reference peer produces it byte for byte**, which is what locates it upstream rather than in a peer — and checking `go` first is the whole diagnostic. The check's own message is right about the peer (*"peer does not serve §5.1 ping once established, so a pre-hello ping is an UNKNOWN operation to it → row 10's 400 invalid_request, not the 409 this row asserts"*), and §3.3's satisfaction-mode language (0.8.2.7) says a check MUST NOT be pinned to a row it cannot reach. | `entity-core-go` `cmd/internal/validate` (`connect_ping_before_hello`, its `pingServedOnceEstablished` sibling control, and the §9.0 profile carve-out list); `ENTITY-CORE-PROTOCOL.md` §5.1, §9.0, §3.3 satisfaction mode | `go` | **Open — surfaced** 2026-09-08 during the candidate-oracle cohort census. **Ask (small):** carve the not-drivable SKIP out under §9.0, or gate the check on the `pingServedOnceEstablished` control the sibling row already uses. **Why it is a live item and not a curiosity:** the oracle exits non-zero, and the five harnesses that propagate that exit code deliberately (`io pd python ruby sql`) DO now exit 1 on a fully green run — the pin was flipped to `78db4a9` on 2026-09-08. We are not adding a `|| true`: those harnesses hold the exit code on purpose, and hiding a non-zero exit is a defect this repo has already recorded in a wrapper. Disclosed in `CONFORMANCE-MATRIX.md` §1's skip note and allowlisted by name (46 entries) in `tools/skip-provenance-gate.py`; removing those entries is part of closing this. |

| **F60** | spec gap — **the §6.13(a) "registered handler, no body" failure has no pinned disposition, and the generated cohort answers it four ways** | Measured on the wire across all 46 peers (`tools/host-seam-probe`, 2026-09-09): an identical `system/handler:register` + dispatch is answered `no_handler_body` (8 peers), `handler_not_found` (7), `unsupported_operation` (4) and `not_implemented` (1, `pd`). §3.3's 501 row names `unsupported_operation` for *an operation absent from the manifest*; here the operation IS in the manifest and the BODY is missing, which §6.13(a) does not give a code for. **Seven codes appear in no normative document at all** (corpus searched by name: the `v0.8.2.11` snapshot plus the entire `entity-system-architecture/specs/` tree — every `EXTENSION-*`, `SDK-*`, `DOMAIN-*`, `GUIDE-*`): `no_outbound_seam` (36 peers), `invalid_resource` (34), `no_handler_body` (32), `invalid_peer_pattern` (31), `expression_not_found` (24), `unsupported_expression` (24) — all six ours, originating in this repo's `go` peer and propagated with the generation lineage — plus `unknown_handler`, which is **oracle-side but permissive**. **Attribution, checked because the opposite was suspected: `entity-core-go` enforces no code absent from the spec.** All 31 codes its validator compares against are either spec-defined or tolerated; `unknown_handler` sits in `registry_issuer.go` (REGISTRY, not core) inside an OR of accepted alternatives, so no peer is gated on it. The ten oracle-asserted codes missing from our *snapshot* are all properly defined in extension specs we deliberately do not vendor — the vendoring boundary working, not a defect. | `ENTITY-CORE-PROTOCOL.md` **0.8.2.11** §3.3 (501 row + the 0.8.2.7 retired spellings + 0.8.2.8 domain-code carve-out), §4.7 (the MUST-emit contract), §6.13(a); `entity-core-go` `cmd/internal/validate/registry_issuer.go:1506`; full write-ups `unspecified-disposition-codes-cohort-vocabulary.md` and `host-seam-dispatch-wire-census.md` | `arch` | **Open — surfaced** 2026-09-09. **Three asks:** (a) pin a disposition for "registered handler, no body" — if it is `unsupported_operation`, that retires `no_handler_body` on 8 peers and settles the other three spellings; (b) rule whether §3.3's retired-spelling list reaches this row, which decides `pd`'s `501 not_implemented` — 0.8.2.8's carve-out for *"a domain code defined for a different failure"* may cover it, so **we have not treated it as a defect** pending the ruling; (c) say whether an implementation may mint a code at all, or MUST fall back to a pinned one — 36 peers emitting a code nothing defines is the one answer that helps nobody. **No conformance number moves:** no vector drives this surface, so the 778-check set structurally cannot see it. Routed to arch **2026-09-09** — that packet is an in-flight process artifact and does not publish, so it is named by date rather than by path. |

Plus one gate-relevant *state* finding (not a spec defect): **RT-6's oracle vector already exists** at
`entity-core-go` `af8a582` (`connectivity_f12.go`, `connectivity` = a core-gate category) and hard-FAILs a
non-401 replayed-`authenticate`. 35 peer targets emit `409 connection_already_established` there; 11 more are
unclassified and at least three appear to carry no established-gate at all. The published 43-peer
`--profile core` 0-FAIL is pinned to `cc1970f` and **does not carry forward** to `af8a582`.

RT-14 audited clean cohort-wide (the rule predates 0.8.1 as `A-CL-009`, pinned per-profile). One
non-conformance-affecting hygiene fix in `smalltalk` (`EcStore>>hexKey:` documented lowercase, produced
uppercase; internal store key only, never on the wire — pinned anyway so it cannot meet the path-hex builder).

RT-13a/RT-13b Part-B classes declared for all 43 in
[`protocol-generator/shared/diagnostics/rt13-write-concurrency-classes.md`](../../protocol-generator/shared/diagnostics/rt13-write-concurrency-classes.md), with
arch's §4.1 anti-under-declaration check run from transport source. Two rows **held, not graded** (`zig` —
serialization point is a doc-comment convention, not a resolvable symbol; `common-lisp` — unclassifiable, no
symbol located) and two S rows carrying a caveat (`oz`, `io` — writer *queue*, not single-context writer).

## Provenance

- **F1, F2** surfaced during the spec-data snapshot (bootstrap step 9); recorded in `protocol-generator/shared/spec-data/v7.56/MANIFEST.md`; both independently verified by architecture and acknowledged in review memo `c0513c8`.
- **F3** surfaced during the pre-FFI discipline review (reading all S1–S5 phase docs end-to-end); fully swept (two more instances found + fixed).
- **F4** ground-truth note added (consolidation pass) after reading the Go reference encoder `core/ecf/ecf.go` first-hand.
- **F5** surfaced building the interim dev oracle over `core/ecf` — `sha256(0xA0)` computed by the real encoder (and confirmed by `sha256sum`) contradicts the spec's pinned Appendix A.1 hash. The exact failure mode F1's fixture work exists to prevent.

## The bidirectional contract (recap)

Operator → Research (this log + per-lang ambiguity log) → Architecture (proposal candidate against `entity-core-architecture/.../proposals/`) → back down as spec-data update / profile field / local decision. The hand-off boundary is described in `AGENTS.md`.

## Related

- Bug-class invariants pinned at design time: `research/diagnostics/conformance-invariants.md` (N1–N8).
