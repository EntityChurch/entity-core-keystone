# Unison content-addressed codebase — regenerated headlessly from the committed
# transcripts + source (.u) + any Share pulls. Never committed (SQLite + blobs).
.unison/
scratch.u
# transcript run outputs are regenerated by `ucm transcript`; keep the .md drivers,
# not the generated *.output.md (unless a specific golden is deliberately committed).
*.output.md
