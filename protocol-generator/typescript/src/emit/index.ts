/**
 * The emit pathway (V7 §6.10 / v7.74 §6.13(c)) — the live hook tree writes produce
 * events on, and the consumer-registration primitive a future extension attaches to.
 */

export * from "./emit-bus.js";
