/**
 * The storage foundation — the content-addressed {@link ContentStore} and the
 * mutable `URI → Hash` {@link EntityTree} location index (V7 §1.7).
 */

export * from "./content-store.js";
export * from "./entity-tree.js";
