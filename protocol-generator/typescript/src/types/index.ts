/**
 * The core type registry — the 53 core/operational/type-bootstrap types the peer
 * publishes at `system/type/*`, rendered natively through the byte-green codec
 * (TYPE-SYSTEM §8–§10).
 */

export * from "./type-definition.js";
export * from "./core-type-registry.js";
