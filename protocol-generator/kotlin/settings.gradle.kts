// entity-core-protocol-kotlin — Gradle (Kotlin DSL) settings.
//
// The idiomatic Kotlin/JVM build (build.gradle.kts) per profile.toml [build]. Built
// + tested INSIDE the kotlin-toolchain container (containers/kotlin-toolchain), fully
// offline against the image's pre-seeded gradle caches (`gradle --offline`).
rootProject.name = "entity-core-protocol-kotlin"
