{
  "$comment": "vcpkg port manifest for entity-core-protocol-cpp (S5 packaging, A-CPP-016). vcpkg is one of the two decentralized C++ package managers the slate names (row 3); this port is AUTHORED + version-pinned, NOT submitted to the vcpkg registry — publishing is an operator action (lifecycle Publishing). Consume as an overlay port: `vcpkg install entity-core-protocol --overlay-ports=packaging/vcpkg`. The `version` field is dotted-numeric (the -pre marker lives in CHANGELOG/README, A-CPP-015).",
  "name": "entity-core-protocol",
  "version": "0.1.0",
  "description": "Entity Core protocol (V7) core peer — native C++23 ECF codec + Ed25519/SHA-256 (libsodium). Generated by entity-core-keystone /entity-rosetta.",
  "homepage": "https://github.com/entity-systems/entity-core-keystone",
  "license": "Apache-2.0",
  "supports": "!windows",
  "dependencies": [
    {
      "name": "libsodium",
      "version>=": "1.0.18"
    },
    {
      "name": "vcpkg-cmake",
      "host": true
    },
    {
      "name": "vcpkg-cmake-config",
      "host": true
    }
  ]
}
