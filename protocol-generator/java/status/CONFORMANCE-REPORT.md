> **v7.75 re-run (oracle `entity-core-go @ 62044c5`).**
> `validate-peer --profile core` → **PASS, 0 FAIL** · **576 total · 291 pass · 196 warn · 0 FAIL · 89 skip.**
> New v7.75 categories scored GREEN: **`resource_bounds`** r1 `413 payload_too_large` PASS · r2 `400 chain_depth_exceeded` PASS · r3 connection-flood WARN (SHOULD, external-admission carve-out); **`concurrency`** 5/5 PASS.
> The only net-new peer change this cycle: an explicit §4.10(b) max-chain-depth (64) pre-check that surfaces **400 `chain_depth_exceeded`** for an over-deep chain BEFORE the per-link authz walk — distinct from the prior 403 `capability_denied` (arch v7.75 ruling: structural excess ≠ authz denial). The numbers below this line predate the v7.75 re-vendor and are retained for history.

---

# entity-core-protocol-java — Conformance Report

**Peer #7** (Java / JVM) · **8th byte-compat impl** · **Phases S2
(codec) + S4 (conformance)** · **Status: 🟢 GREEN — `validate-peer --profile core` PASS, 0
FAIL**

---

## S4 — `validate-peer --profile core` → **PASS** (573 · 0 FAIL)

```
573 total · 289 passed · 195 warned · 0 FAILED · 89 skipped → Result: PASS
```

Machine-verified: `CONFORMANCE-REPORT.json` → `summary.failed == 0`. Peer ID
`2KD6sD8JpEHJ3EaQu2mKCfiQZnkvcDmS8xtvstw9c4dHZm`.

**Command:** `./protocol-generator/java/run-s4.sh` →
`validate-peer -addr 127.0.0.1:7777 -profile core -json-out status/CONFORMANCE-REPORT.json`,
run sealed-offline (`--network=none`) in `entity-core-keystone/java-toolchain:latest`; the Go
`validate-peer` ELF and the JDK-21 host share one loopback.

**Oracle provenance.** `output/s4-oracles/validate-peer`, built `CGO_ENABLED=0 / GOWORK=off`
from a **disposable copy** of the READ-ONLY `entity-core-go` source at HEAD **`749e57e`**
("validate-peer/concurrency: keystone §7b matrix fixes (3 Go-side bugs)"). Tree **clean** (0
uncommitted) at build time. `749e57e` is the mainline's in-flight **committed-but-unpushed
HEAD, 14 ahead of origin/main** (carries §6.11 reentry + §7a wire-gate `9c624aa`) — **must
be re-confirmed when it lands upstream** (A-JAVA-011). Verified `validate_echo_dispatch` /
`dispatch_outbound_reentry` symbols present, `core_register_dispatch_roundtrip` gone.

### Core-profile scoreboard

| Category | P | W | F | S | Note |
|---|--:|--:|--:|--:|---|
| connectivity / encoding | 22 / 6 | 0 | 0 | 0 | §4.1 handshake + ECF wire |
| type_system | 108 | 194 | **0** | 0 | **53/53 §9.5 floor byte-identical**; 194 WARN = non-floor (matched-if-present) |
| handlers | 35 | 0 | **0** | 32 | core get/put/connect/capability; ext handlers auto-skip |
| capability / multisig | 12 / 10 | 0 | 0 | 0 | §6.2 mint/delegate/revoke + §PR-8 V2(a) |
| tree_operations | 24 | 1 | **0** | 31 | core get/put; EXTENSION-TREE §9 ops auto-skip |
| security | 28 | 0 | **0** | 1 | §5 capability/signature chain |
| authz | 6 | 0 | 0 | 2 | §A4-AUTHZ codes; ROLE-ext skips carved out |
| **concurrency** | **5** | 0 | **0** | 0 | **§7b — all 5 PASS** (JDK-21 virtual threads); gates core at this oracle HEAD |
| universal_address_space / peer_canonicalization | 8 / 7 | 0 | 0 | 0 | §1.4 / §1.5 v7.65 |
| negotiation / crypto_agility / format_agility | 4 / 4 / 10 | 0 | 0 | 0 | §4.5 / §4.7 surfaces |
| (extension categories) | 0 | 0 | 0 | ~52 | auto-allowlisted §9.0 carve-outs |
| **Total** | **289** | **195** | **0** | **89** | **Result: PASS** |

The 195 warns are oracle-marked non-§9.5-floor type vocabulary (matched-if-present, not a
core FAIL). The 89 skips are §9.0 extension carve-outs the oracle auto-allowlists (exempt
from the FAIL gate). No FAIL is disguised as a SKIP.

### origination-core 3/3 (`run-origination-core.sh`)

```
[origination]  PASS reference_connect · PASS reference_ready · PASS dispatch_outbound_reentry
Result: PASS (3 total, 3 passed, 0 failed)
```

The §6.11 reentry seam wire-proven over real two-peer TCP: the Java target originates an
outbound EXECUTE back to the validator-as-B over the SAME inbound connection, against a Go
`entity-peer --open-access` reference. dispatch-outbound is a **generic relay** — forwards the
`{value: X}` echo params verbatim and returns the downstream result entity verbatim
(§7b concurrency-gate matrix ruling #2).

### §9.5 53-type floor — 53/53 byte-identical

Rendered from the in-code override table `CoreTypeDefs.java` (generated by
`tools/gen-typedefs.py` from the shared cross-impl `type-registry-shapes.json`); each
content_hash computed by THIS peer's own S2-green codec. Diffed **53/53 byte-identical**
against `type-registry-vectors-v1.diag` by `TypeRegistryTest` (peer-side dual of the S2
corpus) AND independently confirmed by the live oracle's `type_system _match` checks (53/53).

### The grind: 86 → 0 FAIL (3 oracle runs)

1. **Run 1** 86 FAIL → fixed: 85 type_system (full §9.5 53-type render, A-JAVA-008) + 1
   concurrency (dispatch-outbound generic-relay shape, A-JAVA-011).
2. **Run 2** 0 FAIL but 3 un-allowlisted SKIPs → fixed: §1.1 entity `data` is an arbitrary
   ECF value, not a map; scalar `primitive/string` puts were 500'ing (A-JAVA-010).
3. **Run 3** **573 · 289P/195W/0F/89S → PASS.**

All fixes were derived spec-first from V7 + the cohort — the oracle, vectors, and harness
were never doctored. Regression: S3 loopback 11/11 + S2 corpus 69/69 + new TypeRegistryTest
53/53 (full `mvn -o -B test` = 15 tests, 0 failures).

### Reproduce

```sh
# 1. build the oracle ELF from a disposable copy of the read-only go source:
rsync -a --exclude='.git' $REPO/entity-core-go/ $WT/output/go-src-disposable/
podman run --rm -v $WT/output/go-src-disposable:/go-src:Z \
  -v $WT/output/s4-oracles:/out:Z -v $HOME/go/pkg/mod:/root/go/pkg/mod:Z \
  -e GOWORK=off -e GOFLAGS=-mod=mod -e GOTOOLCHAIN=local -e CGO_ENABLED=0 \
  localhost/entity-core-keystone/go:latest \
  bash -c 'cd /go-src/cmd && go build -o /out/validate-peer ./validate-peer && go build -o /out/entity-peer ./entity-peer'

# 2. drive the peer (sealed offline, oracle ELF in the java container):
./protocol-generator/java/run-s4.sh                  # the --profile core gate
./protocol-generator/java/run-origination-core.sh    # the §6.11 reentry 3/3
```

Raw JSON: `status/CONFORMANCE-REPORT.json` (`summary.failed == 0`).

---

## S2 — codec layer · `wire-conformance` (lower bar)

**Phase S2 (codec)** · **Status: GREEN**

## Result

| Corpus | Vendored version | Result |
|---|---|---|
| **ECF codec** (`conformance-vectors-v1.cbor`) | v7.71 (byte-identical to v7.56/v7.70); sha256 `41d68d2d…6a052` | **69/69 PASS, byte-identical, first full run, 0 fixes** |
| **Ed448 RFC-8032 KAT** (agility gate, A-JAVA-002) | v7.71 `KEY-TYPE-ED448-1` pins | **PASS — native pubkey + 114-B signature + §1.5 peer_id all byte-equal; cross-checked vs BouncyCastle** |
| **Ed25519 RFC-8032 KAT** (TEST 1, all-zero seed) | RFC 8032 §7.1 | **PASS — native pubkey byte-equal; SunEC verify** |
| **SHAKE256 KAT** (hand-rolled FIPS-202) | NIST `SHAKE256("")` | **PASS — byte-equal** |

Run in-container, sealed-offline (`--network=none`) via `./run-s2.sh` (full gate:
`mvn -o -B clean test`) or `./run-s2.sh package` (also builds the jar).
Image `entity-core-keystone/java-toolchain:latest` (Temurin JDK 21.0.10+7 LTS +
Apache Maven 3.9.9, all pinned + sha-verified). JUnit 5.11.4 test-scope only.

```
Tests run: 12, Failures: 0, Errors: 0, Skipped: 0
== ECF conformance: 69/69 PASS, 0 FAIL ==
```

## How conformance works here

The `conformance-vectors-v1.cbor` fixture carries its own cross-blessed `canonical`
bytes per vector — it is the Go `wire-conformance` oracle's output
(`build-fixture` / `emit-canonical`, 3-way Go × Rust × Python byte-lock).
The harness (`src/test/.../conformance/ConformanceHarness.java`)
**decodes the fixture with THIS peer's own decoder** (a decoder bug is itself a
conformance failure per ENTITY-CBOR-ENCODING.md §E.3), runs every vector through the
codec, and byte-compares against the embedded `canonical`. Byte-identity to the
fixture == oracle PASS. (Same self-contained mechanism the C#/TS/OCaml/Elixir/Zig/CL
peers used; the Go binary is the fixture producer/cross-blesser, not a runtime
checker — its subcommands are `build-fixture`, `emit-canonical`, `legacy-envelope`.)

## ECF corpus (lower bar) — 69/69

**Seventh** independent native ECF codec to reach 69/69 byte-identical on the first
full run with zero fixes (after C#, TS, OCaml, Elixir, Zig, Common Lisp — S8
convergence holds; the mainstream OO/static idiom converges with the distant ones).
Hand-rolled canonical encoder (`ByteArrayOutputStream` builder) + index-walk decoder
(`src/main/java/.../codec/CanonicalCbor.java`), plus LEB128 varint, Base58,
content-hash, peer-id, and Ed25519 signing (JDK SunEC).

| Category | n | Notes |
|---|---|---|
| `float` | 14 | shortest-float ladder (f16/f32/f64) + Rule-4a specials. `Double.doubleToRawLongBits`/`Float.floatToRawIntBits` give exact IEEE bits; f16 via pure-integer mantissa/exponent test (low-42-bits-zero for normals; integer-scaled-in-[1,1023] for subnormals); -0.0 → `f98000` |
| `int` | 14 | major-0/1 minimization. **uint64 / -2^64 head-form range via `BigInteger`** — Java `long` has no native unsigned, so the value carrier is `BigInteger` (sidesteps the C# `ulong` / OCaml int63 trap entirely; matches the CL/TS-bigint posture via a different mechanism) |
| `map_keys` | 6 | length-first then byte-lexicographic on encoded key bytes (ECF Rule 2 / §3.5); mixed text/byte keys |
| `length` | 8 | definite-length only; **N3** empty-map = `0xA0` (falls out of the generic map encoder, not special-cased) |
| `primitive` | 6 | bool/null/empty containers (`Bool.TRUE`/`Bool.FALSE`/`Null.NULL` sealed nodes keep absent ≠ null ≠ false) |
| `nested` | 4 | entity + envelope carrier shapes |
| `tag_reject` | 5 | **N2** — recursive major-type-6 rejection at any depth, incl. nested in `included` entity data and the bare tag-55799 wire frame; throws `TagRejectedException` (a checked `EntityCodecException` subtype), hard reject |
| `content_hash` | 4 | `varint(format_code) ‖ SHA-256(ECF({type,data}))`; **N1** multi-byte varint prefix (synthetic 0x80 → `80 01`, content_hash.4) |
| `peer_id` | 3 | `CBOR-text(Base58(varint(kt) ‖ varint(ht) ‖ digest))`; N1 multi-byte key_type (128) |
| `signature` | 3 | deterministic Ed25519 over canonical ECF, JDK SunEC `Signature("Ed25519")` |
| `envelope` | 2 | full `{root, included}` ECF under the map-key rules |

(64 `encode_equal` + 5 `decode_reject` = 69 testable; the 2 meta entries carry no
`kind` and are skipped, not counted — same accounting as the cohort's 69/69.)

## Conformance invariants (N1–N4) — enforced + covered

| Invariant | How (file) | Covering vectors |
|---|---|---|
| **N1** LEB128 varints | `codec/Varint.java` — every format-code/key-type/hash-type prefix routed through it | `content_hash.4` (fc 128), `peer_id.3` (kt 128), selftest `varintMultibyte` |
| **N2** tag rejection | `codec/CanonicalCbor.java` `dec` major-6 → `throw TagRejectedException` at any depth | `tag_reject.1–5` (incl. nested-in-included + bare 55799), selftest `bareTagRejected` |
| **N3** empty-map `0xA0` | generic map encoder with 0 entries | `length.2` (`{}` → `a0`), `content_hash.1` (empty-data boundary) |
| **N4** entity fidelity | decoder is structural; byte strings carried as `EcfValue.Bytes` (major 2 ≠ major 3, defensive `byte[]` copy in/out); decode→encode is identity for canonical input | round-trip selftests; original-byte forwarding wired at S3 (peer surface) |

## Ed448 / agility — JDK SunEC sign/verify + native pubkey, KAT GATED + BC cross-checked (A-JAVA-002)

The S1 plan gated trusting the Ed448 path on RFC-8032 byte-equality BEFORE using it
for the agility corpus. **The gate passes** (`SelfTest.ed448Kat`, pins from v7.71
`agility-SEEDS.md` §1.1):

- Ed448 seed `0x42×57` → **57-byte public key byte-equal** to the locked pin
  (`2601850d…3b0e00`).
- Ed448 **114-byte signature** over the §1.1 fixture message **byte-equal** to the
  locked RFC-8032 pin (`0aff7a36…b33400`) — deterministic EdDSA via SunEC.
- `Ed.verify` of that signature passes; `PeerId.fromPublicKey` yields the **§1.5
  SHA-256-form** peer_id `3dR1gAppfHXSGMvPRuAfYkkt4P2C1fvnFYpxPBSQP8RLs4`
  (Ed448 pubkey > 32 B → `hash_type=0x01`, digest = SHA-256(pubkey)).
- **BouncyCastle cross-check** (`BouncyCastleCrossCheckTest`, opt-in/test-scope): the
  native-derived pubkey == BC's `Ed448PublicKeyParameters`, and the SunEC signature
  == BC's `Ed448Signer` output, byte-for-byte. **Decision: SunEC for sign/verify
  (core), native derivation for the raw pubkey; BC confirms both and stays opt-in.**

**Crypto sourcing — the headline (A-JAVA-002 + A-JAVA-007):** the JDK SunEC provider
closes the agility **SIGNATURE** bar for BOTH curves natively (no FFI, no opt-in
sub-library) — the first peer whose vendor stdlib does so. BUT SunEC exposes a public
key only as an `EdECPoint` (y + x-sign-bit) and has **no seed→public-point API**, and
the JDK `MessageDigest` registry ships SHA3-{224,256,384,512} but **no SHAKE256** —
which the RFC-8032 Ed448 seed-expansion requires. So the raw 32-/57-byte public-key
derivation (needed for the §1.5 identity-multihash peer_id + the wire `system/peer`
entity) is **hand-rolled** (`EdKeyDerivation` + `Shake256`): SHA-512 (JDK) /
hand-rolled SHAKE256 + `BigInteger` Edwards scalar multiply, keeping the whole core
**zero-runtime-dependency and BouncyCastle-free**. This is the same hand-roll-the-
missing-primitive stance as the CBOR/base58/varint layer. See A-JAVA-007.

## Uncovered-range self-tests (codec-review heuristic) — `SelfTest.java`, all PASS

A green corpus proves the math vs the corpus, not the ranges it doesn't cover:

- **uint64 = 2^64-1 and 2^63** (above signed-i64 max — `BigInteger` carrier; no
  `long`-unsigned trap): encode (`1bffffffffffffffff`) + round-trip correct.
- **nint min -2^64** (`3bffffffffffffffff`).
- float ladder boundaries (1.5→f16, 65503→f32, 1.1→f64, f16 65504 round-trip);
  NaN/±Inf/-0.0 sentinel round-trips.
- peer-id format→parse round-trip (incl. multi-byte key_type 128).
- base58 decode∘encode with leading-zero preservation.
- Ed25519 sign/verify + tamper-reject; **Ed25519 RFC-8032 TEST 1 pubkey byte-equal**.
- **SHAKE256 NIST known-answer** (the hand-rolled XOF).
- N2 bare-tag (55799) rejection.

## Notes

- **Native codec, no FFI, ZERO runtime deps.** Ed25519/Ed448 sign+verify = SunEC;
  SHA-256/384/512 = SunMessageDigest; SHAKE256 + base58 + CBOR + varint + Edwards
  scalar mult = hand-rolled in-repo. The core artifact pulls nothing at runtime.
- **Clean compile.** 19 main + 4 test source files, `javac --release 21`, no warnings.
- **Java seams handled:** `byte[]` defensive-copy at every boundary (`EcfValue.Bytes`
  clones in/out, never aliases — profile `no_byte_array_aliasing`); `BigInteger` for
  the full uint64/-2^64 range (no native unsigned `long`); lowercase `%02x` hex
  everywhere (avoids the A-CL-009 uppercase address-space-path trap by default —
  exercised at S3).
- **Agility matrix beyond the KAT** (MATRIX-M2/M3/M6, cap-token content_hash) is S3
  peer-layer work (needs the §3.6 cap-token shape + key registry) — same deferral as
  the cohort. The Ed448+SHA-384 primitives are proven at S2.

## Exit criteria

ECF corpus byte-identical (69/69) · Ed448 KAT byte-equal + BC cross-checked (A-JAVA-002
gate passes) · Ed25519 + SHAKE256 KATs pass · selftests pass · `mvn` compiles clean
(no warnings) · ambiguity log has no blocking codec items · container reproducible
(JDK sha256 + Maven sha512 filled + verified, deps pre-fetched offline). **S2 PASS.**

---

## S5 verdict (publish)

**PUBLISH-READY at `0.1.0-pre`, NOT published.** S4 `--profile core` PASS (573 · 289P/195W/
**0F**/89S, machine-verified `summary.failed == 0`) stands as the conformance contract; S5
added the release artifacts (`README.md`, `CHANGELOG.md`, `LICENSE`, `pom.xml` version
`0.1.0-pre` with license/developer metadata) and the architecture review
(`status/ARCHITECTURE-REVIEW.md` — Java as the 9th byte-compatible core peer; mainstream
static-OO idiom axis; the SunEC crypto-agility data point; the consolidated A-JAVA-### findings
ledger). Maven Central deploy is **deferred** (verified `org.entitycore` namespace required —
A-JAVA-005 — plus arch v0.1 sign-off + a first external consumer). **Open for the merge
decision (A-JAVA-011):** the core gate count moved 568→573 because §7b now gates `--profile
core` at the in-flight oracle HEAD `749e57e`; needs an arch ruling (is §7b-gates-core intended
for v0.1?) and a re-confirm of Java's 573·0F once `749e57e` lands on origin/main. Verdict is
0 FAIL either way (568-subset or 573-superset). See `status/PHASE-S5.md`.
