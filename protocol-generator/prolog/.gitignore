# S2-FFI build outputs (regenerated by run-s2.sh inside the prolog-toolchain
# container). The C-ABI codec lib + the SWI foreign shim .so are byte-built, not
# committed — only the sources (c/, prolog/, test/) and the build script are.
build/
prolog/ec_codec_pl.so
*.so
*.o

# S4 conformance: the vendored Go oracle ELFs (validate-peer, entity-peer,
# @75c532e) live here, gitignored — they are large binaries, not peer sources.
output/
